# 📱 회원가입 화면

## 개요

새로운 사용자가 계정을 생성하는 화면입니다.

---

## 접근 경로

| 항목 | 값 |
|------|-----|
| **URL** | `/sign-up` |
| **라우트 파일** | `src/app/(auth)/sign-up/page.jsx` |
| **스타일** | `src/styles/auth/sign-up.module.css` |

---

## 레이아웃 구조

### 전체 레이아웃

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│           ┌───────────────────────────┐             │
│           │         Logo              │             │
│           │    "함께, 더 높이"         │             │
│           │                           │             │
│           │   ┌─────────────────────┐ │             │
│           │   │  [프로필 이미지]     │ │             │
│           │   └─────────────────────┘ │             │
│           │   ┌─────────────────────┐ │             │
│           │   │  이름 입력            │ │             │
│           │   └─────────────────────┘ │             │
│           │   ┌─────────────────────┐ │             │
│           │   │  이메일 입력          │ │             │
│           │   └─────────────────────┘ │             │
│           │   ┌─────────────────────┐ │             │
│           │   │  비밀번호 입력        │ │             │
│           │   │  [강도 표시]          │ │             │
│           │   └─────────────────────┘ │             │
│           │   ┌─────────────────────┐ │             │
│           │   │  비밀번호 확인        │ │             │
│           │   └─────────────────────┘ │             │
│           │                           │             │
│           │   [      회원가입       ] │             │
│           │                           │             │
│           │   ───── 또는 ─────       │             │
│           │                           │             │
│           │   [Google로 가입]        │             │
│           │   [GitHub로 가입]        │             │
│           │                           │             │
│           │   이미 계정이 있으신가요? 로그인 │        │
│           └───────────────────────────┘             │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 영역별 상세

### 회원가입 폼

| 요소 | 설명 |
|------|------|
| 로고 | 클릭 시 홈으로 이동 |
| 프로필 이미지 | 선택적, 5MB 이하 이미지 |
| 이름 입력 | 최소 2자 |
| 이메일 입력 | 이메일 형식 검증 |
| 비밀번호 입력 | 최소 8자, 강도 표시 |
| 비밀번호 확인 | 비밀번호 일치 검증 |
| 회원가입 버튼 | 폼 제출 |
| 소셜 가입 | Google, GitHub 버튼 |
| 로그인 링크 | `/sign-in`으로 이동 |

---

## 상태 관리

### 폼 상태

| 상태 | 타입 | 설명 |
|------|------|------|
| `name` | string | 이름 입력값 |
| `email` | string | 이메일 입력값 |
| `password` | string | 비밀번호 입력값 |
| `confirmPassword` | string | 비밀번호 확인 |
| `avatar` | File \| null | 프로필 이미지 파일 |
| `avatarPreview` | string \| null | 미리보기 URL |
| `showPassword` | boolean | 비밀번호 표시 여부 |
| `showConfirmPassword` | boolean | 비밀번호 확인 표시 여부 |

### UI 상태

| 상태 | 타입 | 설명 |
|------|------|------|
| `loading` | boolean | 로딩 상태 |
| `error` | string \| null | 에러 메시지 |
| `formErrors` | object | 필드별 에러 |
| `passwordStrength` | string \| null | 비밀번호 강도 |
| `uploadingAvatar` | boolean | 이미지 업로드 중 |

---

## 유효성 검사

### 필드별 검증 규칙

| 필드 | 규칙 | 에러 메시지 |
|------|------|-------------|
| `name` | 최소 2자 | 이름은 2자 이상이어야 합니다 |
| `email` | 이메일 형식 | 올바른 이메일 형식이 아닙니다 |
| `password` | 최소 8자 | 비밀번호는 8자 이상이어야 합니다 |
| `password` | 복잡성 | 영문, 숫자, 특수문자 중 2가지 이상 포함 |
| `confirmPassword` | 일치 | 비밀번호가 일치하지 않습니다 |
| `avatar` | 이미지 형식 | 이미지 파일만 업로드 가능합니다 |
| `avatar` | 5MB 이하 | 파일 크기는 5MB 이하여야 합니다 |

### 비밀번호 강도 계산

```javascript
const calculatePasswordStrength = (pwd) => {
  if (pwd.length < 8) return 'weak'
  
  let strength = 0
  if (/[a-z]/.test(pwd)) strength++
  if (/[A-Z]/.test(pwd)) strength++
  if (/[0-9]/.test(pwd)) strength++
  if (/[^a-zA-Z0-9]/.test(pwd)) strength++
  
  if (strength >= 3 && pwd.length >= 12) return 'strong'
  if (strength >= 2 && pwd.length >= 8) return 'medium'
  return 'weak'
}
```

| 강도 | 조건 | 표시 |
|------|------|------|
| `weak` | 8자 미만 또는 단순 | 🔴 약함 |
| `medium` | 8자 이상 + 2종류 이상 | 🟡 보통 |
| `strong` | 12자 이상 + 3종류 이상 | 🟢 강함 |

---

## 회원가입 처리

### 처리 흐름

```javascript
const handleSubmit = async (e) => {
  e.preventDefault()
  
  if (!validateForm()) return
  
  setLoading(true)
  
  try {
    // 1. 아바타 업로드 (선택적)
    let avatarUrl = null
    if (avatar) {
      avatarUrl = await uploadAvatar()
    }
    
    // 2. 회원가입 API 호출
    const response = await fetch('/api/auth/signup', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        name,
        email,
        password,
        avatar: avatarUrl
      })
    })
    
    if (response.ok) {
      // 3. 로그인 페이지로 이동
      router.push('/sign-in?message=회원가입이 완료되었습니다')
    }
  } catch (error) {
    setError(error.message)
  } finally {
    setLoading(false)
  }
}
```

---

## 프로필 이미지 처리

### 이미지 선택

```javascript
const handleAvatarChange = (e) => {
  const file = e.target.files?.[0]
  if (!file) return

  // 파일 타입 검증
  if (!file.type.startsWith('image/')) {
    setFormErrors({ avatar: '이미지 파일만 업로드 가능합니다' })
    return
  }

  // 파일 크기 검증 (5MB)
  if (file.size > 5 * 1024 * 1024) {
    setFormErrors({ avatar: '파일 크기는 5MB 이하여야 합니다' })
    return
  }

  setAvatar(file)

  // 미리보기 생성
  const reader = new FileReader()
  reader.onloadend = () => {
    setAvatarPreview(reader.result)
  }
  reader.readAsDataURL(file)
}
```

---

## 이미 로그인된 사용자 처리

```javascript
useEffect(() => {
  if (status === 'authenticated') {
    router.push(callbackUrl)
  }
}, [status, router, callbackUrl])
```

---

## 사용 컴포넌트

| 컴포넌트 | 소스 | 용도 |
|---------|------|------|
| `Image` | next/image | 로고, 프로필 미리보기 |
| `Link` | next/link | 네비게이션 |

---

## 관련 문서

- [로그인 화면](./screens-sign-in.md) - 로그인
- [인증 컴포넌트](./components.md) - 공통 컴포넌트
- [인증 API](./api-endpoints.md) - 회원가입 API

