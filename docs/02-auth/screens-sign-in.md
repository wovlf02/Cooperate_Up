# 📱 로그인 화면

## 개요

사용자가 이메일/비밀번호 또는 소셜 로그인으로 인증하는 화면입니다.

---

## 접근 경로

| 항목 | 값 |
|------|-----|
| **URL** | `/sign-in` |
| **라우트 파일** | `src/app/(auth)/sign-in/page.jsx` |
| **스타일** | `src/styles/auth/sign-in.module.css` |

---

## 레이아웃 구조

### 전체 레이아웃

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│           ┌───────────────────────────┐             │
│           │         Logo              │             │
│           │    "함께, 더 높이"         │             │
│           │                           │             │
│           │   ┌─────────────────────┐ │             │
│           │   │  이메일 입력          │ │             │
│           │   └─────────────────────┘ │             │
│           │   ┌─────────────────────┐ │             │
│           │   │  비밀번호 입력        │ │             │
│           │   └─────────────────────┘ │             │
│           │                           │             │
│           │   [       로그인        ] │             │
│           │                           │             │
│           │   ───── 또는 ─────       │             │
│           │                           │             │
│           │   [Google로 로그인]      │             │
│           │   [GitHub로 로그인]      │             │
│           │                           │             │
│           │   계정이 없으신가요? 가입  │             │
│           └───────────────────────────┘             │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 영역별 상세

### 로그인 폼

| 요소 | 설명 |
|------|------|
| 로고 | 클릭 시 홈으로 이동 |
| 슬로건 | "함께, 더 높이" |
| 이메일 입력 | 이메일 형식 검증 |
| 비밀번호 입력 | 최소 8자, 표시/숨김 토글 |
| 로그인 버튼 | Credentials 로그인 |
| 소셜 로그인 | Google, GitHub 버튼 |
| 회원가입 링크 | `/sign-up`으로 이동 |

### 에러 표시

| 에러 타입 | 메시지 |
|-----------|--------|
| 검증 오류 | 각 필드 아래 표시 |
| 인증 오류 | 폼 상단에 표시 |
| URL 파라미터 | `?error=...` 처리 |

---

## 상태 관리

### 폼 상태

| 상태 | 타입 | 설명 |
|------|------|------|
| `email` | string | 이메일 입력값 |
| `password` | string | 비밀번호 입력값 |
| `showPassword` | boolean | 비밀번호 표시 여부 |

### UI 상태

| 상태 | 타입 | 설명 |
|------|------|------|
| `loading` | string \| null | 로딩 중인 Provider |
| `error` | string \| null | 에러 메시지 |
| `formErrors` | object | 필드별 에러 |

### 세션 상태

```javascript
const { data: session, status } = useSession()
```

---

## 유효성 검사

### 클라이언트 검증

```javascript
const validateForm = () => {
  const errors = {}
  
  if (!email) {
    errors.email = '이메일을 입력해주세요'
  } else if (!validateEmail(email)) {
    errors.email = '올바른 이메일 형식이 아닙니다'
  }
  
  if (!password) {
    errors.password = '비밀번호를 입력해주세요'
  } else if (password.length < 8) {
    errors.password = '비밀번호는 8자 이상이어야 합니다'
  }
  
  return Object.keys(errors).length === 0
}
```

---

## 로그인 처리

### Credentials 로그인

```javascript
const result = await signIn('credentials', {
  email,
  password,
  redirect: false,
})

if (result?.ok) {
  // 관리자 권한 확인 후 리다이렉트
  const userData = await api.get('/api/auth/me')
  
  if (userData.adminRole) {
    router.push('/admin')
  } else {
    router.push('/dashboard')
  }
}
```

### 이미 로그인된 사용자 처리

```javascript
useEffect(() => {
  if (status === 'authenticated' && session?.user?.id) {
    // 세션 유효성 검증
    api.get('/api/auth/validate-session')
      .then(data => {
        if (data.valid) {
          // 관리자 여부에 따라 리다이렉트
        } else if (data.shouldLogout) {
          // 세션 무효 - 로그아웃 처리
          signOut({ redirect: false })
        }
      })
  }
}, [status, session])
```

---

## URL 파라미터 처리

| 파라미터 | 값 | 동작 |
|----------|-----|------|
| `callbackUrl` | URL | 로그인 후 이동할 경로 |
| `error` | `account-deleted` | 삭제된 계정 메시지 |
| `error` | `account-suspended` | 정지된 계정 메시지 |
| `error` | `CredentialsSignin` | 인증 실패 메시지 |

---

## 사용 컴포넌트

| 컴포넌트 | 소스 | 용도 |
|---------|------|------|
| `Image` | next/image | 로고 표시 |
| `Link` | next/link | 네비게이션 |

---

## 관련 문서

- [회원가입 화면](./screens-sign-up.md) - 회원가입
- [인증 컴포넌트](./components.md) - 공통 컴포넌트
- [NextAuth 설정](./api-nextauth.md) - 인증 설정

