# 🖥️ 대시보드 화면

## 개요

사용자의 전체 활동을 한눈에 볼 수 있는 메인 대시보드 화면입니다.

**파일 위치**: 
- `src/app/dashboard/page.jsx` (서버 컴포넌트)
- `src/components/dashboard/DashboardClient.jsx` (클라이언트 컴포넌트)

---

## 화면 구성

```
┌─────────────────────────────────────────────────────────────┐
│  📊 대시보드                                                 │
│  나의 활동을 한눈에 확인하세요                               │
├─────────────────────────────────────────────────────────────┤
│  안녕하세요, 홍길동님! 👋                                    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │   📚    │  │   ✅    │  │   🔔    │  │   🎯    │        │
│  │활성 스터디│  │진행 중  │  │읽지 않음│  │이번 달  │        │
│  │    3    │  │    5    │  │    2    │  │   12   │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
├─────────────────────────────────────────────────────────────┤
│  내 스터디                                    [전체 보기 →]  │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                     │
│  │   📚    │  │   💻    │  │   📖    │                     │
│  │알고리즘 │  │ React   │  │ 영어    │                     │
│  │   IT    │  │   IT    │  │  어학   │                     │
│  │👑 3명   │  │👤 5명   │  │👤 4명   │                     │
│  └─────────┘  └─────────┘  └─────────┘                     │
├─────────────────────────────────────────────────────────────┤
│  알림                                        [전체 보기 →]  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 📚 새 메시지가 도착했습니다                            │  │
│  │    알고리즘 스터디                          3분 전     │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ 📢 새 공지사항이 등록되었습니다                        │  │
│  │    React 스터디                            1시간 전    │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  다가오는 일정                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 15  │ 📚 정기 모임                                    │  │
│  │ 12월│ 알고리즘 스터디                                 │  │
│  │     │ 14:00 - 16:00                                   │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐                   │
│  │ 📊 스터디 현황  │  │ ⚡ 빠른 액션   │                   │
│  │ 🎯 D-3          │  │ [스터디 찾기]  │                   │
│  │ 👥 출석률 85%   │  │ [할일 추가]    │                   │
│  │ ✅ 할일 75%     │  │ [알림 보기]    │                   │
│  │ 🔥 7일 연속     │  │                │                   │
│  └─────────────────┘  └─────────────────┘                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 컴포넌트 구조

```jsx
<DashboardPage>                    // 서버 컴포넌트
  └── <DashboardErrorBoundary>     // 에러 경계
      └── <DashboardClient>        // 클라이언트 컴포넌트
          ├── Header               // 헤더 (제목, 부제목)
          ├── WelcomeSection       // 환영 메시지
          ├── StatsGrid            // 통계 카드 4개
          ├── MyStudiesSection     // 내 스터디 그리드
          │   ├── EmptyState       // 스터디 없을 때
          │   └── StudyCard[]      // 스터디 카드들
          ├── ActivitiesSection    // 알림 섹션
          │   ├── EmptyState       // 알림 없을 때
          │   └── ActivityItem[]   // 알림 항목들
          ├── EventsSection        // 다가오는 일정
          │   └── EventItem[]      // 일정 항목들
          └── Widgets              // 위젯들
              ├── StudyStatus      // 스터디 현황
              ├── OnlineMembers    // 온라인 멤버
              ├── QuickActions     // 빠른 액션
              ├── UrgentTasks      // 긴급 할일
              └── PinnedNotice     // 고정 공지
</DashboardPage>
```

---

## 서버 컴포넌트

```jsx
// src/app/dashboard/page.jsx
export default async function DashboardPage() {
  const session = await getSession();
  
  if (!session) {
    redirect('/sign-in');
  }

  return <DashboardClient user={session.user} />;
}
```

---

## 클라이언트 컴포넌트

### API Hooks

```jsx
const { data: dashboardData, isLoading } = useDashboard();
const { data: userData, isLoading: userLoading } = useMe();

// 최신 사용자 정보 사용
const user = userData?.user || initialUser;
```

### 통계 카드

```jsx
const statsCards = useMemo(() => [
  { icon: '📚', label: '활성 스터디', value: stats.activeStudies, color: 'blue' },
  { icon: '✅', label: '진행 중인 할일', value: stats.pendingTasks, color: 'green' },
  { icon: '🔔', label: '읽지 않은 알림', value: stats.unreadNotifications, color: 'yellow' },
  { icon: '🎯', label: '이번 달 완료', value: stats.completedThisMonth, color: 'purple' }
], [stats]);
```

---

## 로딩 상태

```jsx
if (isLoading || userLoading) {
  return <DashboardSkeleton />;
}

if (!dashboardData?.data) {
  return (
    <EmptyState
      icon="⚠️"
      title="데이터를 불러올 수 없습니다"
      description="잠시 후 다시 시도해주세요"
    />
  );
}
```

---

## 빈 상태 컴포넌트

```jsx
<EmptyState
  icon="📚"
  title="참여 중인 스터디가 없습니다"
  description="새로운 스터디를 찾아보세요!"
  actionText="스터디 탐색하기"
  actionHref="/studies"
/>
```

---

## 에러 경계

```jsx
<DashboardErrorBoundary userId={user?.id}>
  <div className={styles.container}>
    {/* 대시보드 내용 */}
  </div>
</DashboardErrorBoundary>
```

---

## 스타일

**파일**: `src/app/dashboard/page.module.css`

### 주요 클래스

| 클래스 | 설명 |
|--------|------|
| `.container` | 전체 컨테이너 |
| `.statsGrid` | 통계 카드 그리드 (4열) |
| `.statCard` | 개별 통계 카드 |
| `.studiesGrid` | 스터디 카드 그리드 |
| `.studyCard` | 개별 스터디 카드 |
| `.activitiesList` | 알림 목록 |
| `.activityItem` | 개별 알림 항목 |
| `.activityItem.unread` | 읽지 않은 알림 |
| `.eventsList` | 일정 목록 |
| `.eventItem` | 개별 일정 항목 |

---

## 유틸리티 함수

### 상대 시간 포맷

```jsx
function formatRelativeTime(dateString) {
  const date = new Date(dateString);
  const now = new Date();
  const diffMs = now - date;
  const diffMins = Math.floor(diffMs / 60000);
  
  if (diffMins < 1) return '방금 전';
  if (diffMins < 60) return `${diffMins}분 전`;
  if (diffMins < 1440) return `${Math.floor(diffMins / 60)}시간 전`;
  return `${Math.floor(diffMins / 1440)}일 전`;
}
```

### D-day 계산

```jsx
function calculateDday(dateString) {
  const eventDate = new Date(dateString);
  const today = new Date();
  const diffMs = eventDate - today;
  return Math.ceil(diffMs / (1000 * 60 * 60 * 24));
}
```

---

## 관련 문서

- [README](./README.md)
- [API](./api.md)
- [위젯](./widgets.md)
- [예외](./exceptions.md)

