# 📅 캘린더 도메인

## 개요

스터디 내 일정 관리 기능입니다. 월/주/일 뷰와 리스트 뷰를 제공합니다.

---

## 주요 기능

| 기능 | 설명 |
|------|------|
| 일정 조회 | 월/주/일 캘린더 뷰, 리스트 뷰 |
| 일정 생성 | ADMIN 이상 권한 필요 |
| 일정 수정 | 작성자 또는 ADMIN/OWNER |
| 일정 삭제 | 작성자 또는 ADMIN/OWNER |
| 중복 확인 | 같은 시간대 일정 경고 |

---

## 아키텍처

```
┌────────────────────────────────────────────────────┐
│                    Client                          │
│  ┌──────────────────────────────────────────────┐  │
│  │         CalendarPage (page.jsx)              │  │
│  │  - 캘린더 뷰 (월/주/일)                       │  │
│  │  - 리스트 뷰                                  │  │
│  │  - 일정 추가/수정/삭제 모달                   │  │
│  └──────────────────────────────────────────────┘  │
│                        │                           │
│                        ▼                           │
│  ┌──────────────────────────────────────────────┐  │
│  │          useApi Hooks                        │  │
│  │  - useEvents()                               │  │
│  │  - useCreateEvent()                          │  │
│  │  - useUpdateEvent()                          │  │
│  │  - useDeleteEvent()                          │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────┬──────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────┐
│                     Server                         │
│  ┌──────────────────────────────────────────────┐  │
│  │       /api/studies/[id]/calendar             │  │
│  │  - GET:  일정 목록 조회                       │  │
│  │  - POST: 일정 생성                            │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │   /api/studies/[id]/calendar/[eventId]       │  │
│  │  - GET:    일정 상세 조회                     │  │
│  │  - PATCH:  일정 수정                          │  │
│  │  - DELETE: 일정 삭제                          │  │
│  └──────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

---

## 파일 구조

```
coup/src/
├── app/
│   ├── api/studies/[id]/calendar/
│   │   ├── route.js              # GET(목록), POST(생성)
│   │   └── [eventId]/
│   │       └── route.js          # GET(상세), PATCH(수정), DELETE(삭제)
│   └── my-studies/[studyId]/calendar/
│       ├── page.jsx              # 캘린더 화면
│       └── page.module.css       # 스타일
└── lib/
    ├── hooks/useApi.js           # useEvents, useCreateEvent, ...
    └── exceptions/study/
        └── StudyException.js     # 일정 관련 예외
```

---

## 데이터베이스 모델

### Event 모델

```prisma
model Event {
  id          String   @id @default(cuid())
  studyId     String
  createdById String
  title       String
  date        DateTime
  startTime   String   // "HH:MM" 형식
  endTime     String   // "HH:MM" 형식
  location    String?
  description String?
  color       String   @default("#6366F1")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  study       Study    @relation(fields: [studyId], references: [id], onDelete: Cascade)
  createdBy   User     @relation(fields: [createdById], references: [id])
}
```

---

## 뷰 모드

| 뷰 | 설명 |
|----|------|
| 월 (month) | 달력 형태로 전체 월 표시 |
| 주 (week) | 일요일~토요일 7일 표시, 시간대별 그리드 |
| 일 (day) | 하루 24시간 타임라인 |
| 리스트 (list) | 날짜별 그룹화된 목록 |

---

## 권한

| 역할 | 조회 | 생성 | 수정 | 삭제 |
|------|------|------|------|------|
| OWNER | ✅ | ✅ | ✅ 모든 일정 | ✅ 모든 일정 |
| ADMIN | ✅ | ✅ | ✅ 모든 일정 | ✅ 모든 일정 |
| MEMBER | ✅ | ❌ | ✅ 본인 일정 | ✅ 본인 일정 |

---

## 관련 문서

- [API](./api.md)
- [화면](./screens.md)
- [예외](./exceptions.md)

