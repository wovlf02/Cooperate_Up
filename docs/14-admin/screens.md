# 🖥️ 관리자 화면

## 개요

관리자 페이지 화면 구성입니다.

**파일 위치**: `src/app/admin/`

---

## 화면 목록

| 경로 | 파일 | 설명 |
|------|------|------|
| `/admin` | page.jsx | 대시보드 |
| `/admin/users` | users/ | 사용자 관리 |
| `/admin/studies` | studies/ | 스터디 관리 |
| `/admin/reports` | reports/ | 신고 관리 |
| `/admin/analytics` | analytics/ | 분석 |
| `/admin/audit-logs` | audit-logs/ | 감사 로그 |
| `/admin/settings` | settings/ | 설정 |

---

## 대시보드 (/admin)

```
┌─────────────────────────────────────────────────────────────┐
│  📊 대시보드                                                 │
│  플랫폼 현황을 한눈에 확인하세요                             │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 👥 총   │  │ 📚 활성 │  │ ⚠️ 처리 │  │ 🆕 신규 │        │
│  │ 사용자  │  │ 스터디  │  │ 대기    │  │ 가입    │        │
│  │ 1,000명 │  │  250개  │  │  10건   │  │  85명   │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────┐ ┌──────────────────────────┐  │
│  │ 📈 최근 활동            │ │ ⚡ 빠른 작업             │  │
│  │ - 신규 사용자 가입      │ │ [사용자 관리]           │  │
│  │ - 신고 접수             │ │ [스터디 관리]           │  │
│  │ - 경고 발송             │ │ [신고 관리]             │  │
│  └──────────────────────────┘ └──────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 레이아웃 구조

### AdminLayout

```jsx
<AdminLayout>
  ├── <AdminNavbar />
  │   ├── Logo
  │   ├── 검색
  │   ├── 알림
  │   └── 프로필
  ├── <Sidebar />
  │   ├── 대시보드
  │   ├── 사용자 관리
  │   ├── 스터디 관리
  │   ├── 신고 관리
  │   ├── 분석
  │   ├── 감사 로그
  │   └── 설정
  └── <main>
      {children}
  </main>
</AdminLayout>
```

---

## 컴포넌트

### 공통 컴포넌트

| 컴포넌트 | 파일 | 설명 |
|----------|------|------|
| AdminNavbar | common/AdminNavbar.jsx | 상단 네비게이션 |
| Sidebar | common/Sidebar.jsx | 사이드바 메뉴 |
| SearchBar | common/SearchBar.jsx | 검색 바 |
| FilterPanel | common/FilterPanel.jsx | 필터 패널 |
| Breadcrumb | common/Breadcrumb.jsx | 브레드크럼 |

### UI 컴포넌트

| 컴포넌트 | 파일 | 설명 |
|----------|------|------|
| Button | ui/Button.jsx | 버튼 |
| Card | ui/Card.jsx | 카드 |
| StatCard | ui/Stats.jsx | 통계 카드 |
| Toast | ui/Toast/ | 토스트 알림 |

---

## 사용자 관리 (/admin/users)

### 기능

- 사용자 목록 조회 (페이지네이션, 검색, 필터)
- 사용자 상세 정보
- 경고 발송
- 계정 정지/해제
- 계정 삭제

### 필터 옵션

| 필터 | 값 |
|------|-----|
| 상태 | ACTIVE, SUSPENDED, DELETED |
| 가입 방식 | GOOGLE, KAKAO, NAVER, EMAIL |
| 경고 여부 | 있음/없음 |
| 가입일 | 날짜 범위 |
| 마지막 로그인 | 날짜 범위 |

---

## 스터디 관리 (/admin/studies)

### 기능

- 스터디 목록 조회
- 스터디 상세 정보
- 스터디 숨김/공개
- 스터디 종료
- 스터디 삭제

---

## 신고 관리 (/admin/reports)

### 기능

- 신고 목록 조회
- 신고 상세 정보
- 담당자 할당
- 신고 처리 (경고, 정지, 삭제)
- 신고 해결/거부

### 상태

| 상태 | 설명 |
|------|------|
| PENDING | 대기 중 |
| IN_PROGRESS | 처리 중 |
| RESOLVED | 해결됨 |
| REJECTED | 거부됨 |

---

## 분석 (/admin/analytics)

### 기능

- 사용자 증가 추이
- 스터디 생성 추이
- 활동 통계
- 데이터 내보내기

---

## 감사 로그 (/admin/audit-logs)

### 기록 항목

| 항목 | 설명 |
|------|------|
| 관리자 ID | 작업 수행자 |
| 작업 유형 | USER_SUSPEND, REPORT_RESOLVE 등 |
| 대상 | 대상 사용자/스터디/신고 ID |
| 상세 정보 | 변경 내용 |
| IP 주소 | 요청 IP |
| 타임스탬프 | 작업 시간 |

---

## 상태 관리

```jsx
const [stats, setStats] = useState(null);
const [loading, setLoading] = useState(true);
const [error, setError] = useState(null);

useEffect(() => {
  if (status === 'authenticated') {
    fetchStats();
  }
}, [status]);

async function fetchStats() {
  try {
    setLoading(true);
    const result = await api.get('/api/admin/stats');
    setStats(result.success ? result.data : null);
    setError(null);
  } catch (err) {
    setError(err.message);
  } finally {
    setLoading(false);
  }
}
```

---

## 관련 문서

- [README](./README.md)
- [API](./api.md)
- [권한](./permissions.md)
- [예외](./exceptions.md)

