# 근로계약서 화면 상세 설계

> **Production Ready v2.0** - 프로덕션 수준의 UX/UI 명세

## 🎯 UX 목표
- **법적 준수**: 대한민국 표준 근로계약서 양식 자동 생성
- **간편한 작성**: 최소한의 입력으로 완전한 계약서 생성
- **즉각적 검증**: 법규 위반 사항 실시간 검증 및 경고
- **원터치 서명**: 터치 기반 전자서명으로 간편한 계약 체결

---

## 1. ContractListScreen (계약서 목록)

### 1.1 화면 개요
| 항목 | 내용 |
|------|------|
| 경로 | /contract |
| 권한 | 공통 |
| 설명 | 계약서 목록 조회 및 필터링 |

### 1.2 레이아웃

```
┌─────────────────────────────────────────┐
│ StatusBar                               │
├─────────────────────────────────────────┤
│ Header                                  │
│ ┌─────────────────────────────────────┐ │
│ │ ←   근로계약서                      │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│                                         │
│  Filter Tabs (필터 탭)                  │
│ ┌─────────────────────────────────────┐ │
│ │  ┌──────┬──────┬──────┬──────┐     │ │
│ │  │ 전체 │서명  │ 완료 │ 만료 │     │ │
│ │  │      │대기  │      │      │     │ │
│ │  └──────┴──────┴──────┴──────┘     │ │
│ │  backgroundColor: neutral100        │ │
│ │  borderRadius: borderRadius.lg      │ │
│ └─────────────────────────────────────┘ │
│                                         │
│  Contract List (FlatList)               │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ 📄 2024년 1월 근로계약서    │    │ │
│ │  │                             │    │ │
│ │  │ 👤 김직원                   │    │ │
│ │  │ 📅 2024.01.01 ~ 2024.12.31  │    │ │
│ │  │                             │    │ │
│ │  │ ┌────────────┐              │    │ │
│ │  │ │ ✅ 서명완료 │          >  │    │ │
│ │  │ │ success50  │              │    │ │
│ │  │ └────────────┘              │    │ │
│ │  │                             │    │ │
│ │  │ borderRadius: borderRadius.xl    │ │
│ │  │ shadows.sm                  │    │ │
│ │  └─────────────────────────────┘    │ │
│ │  gap: spacing.space3                │ │
│ │                                     │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ 📄 2024년 1월 근로계약서    │    │ │
│ │  │                             │    │ │
│ │  │ 👤 이직원                   │    │ │
│ │  │ 📅 2024.01.15 ~             │    │ │
│ │  │                             │    │ │
│ │  │ ┌────────────┐              │    │ │
│ │  │ │ ⏳ 서명대기 │          >  │    │ │
│ │  │ │ warning50  │              │    │ │
│ │  │ └────────────┘              │    │ │
│ │  └─────────────────────────────┘    │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                         │
│  (사업주만 표시 - FAB)                  │
│        ┌────────────────────┐           │
│        │   + 계약서 작성    │           │
│        │   shadows.lg       │           │
│        └────────────────────┘           │
│                                         │
└─────────────────────────────────────────┘
```

### 1.3 컴포넌트 스타일

```typescript
const contractCardStyles = {
  container: {
    backgroundColor: colors.white,
    borderRadius: borderRadius.xl,
    padding: spacing.space5,
    marginHorizontal: layout.screenPadding,
    ...shadows.sm,
  },
  
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: spacing.space3,
  },
  
  icon: {
    fontSize: ms(24),
    marginRight: spacing.space3,
  },
  
  title: {
    ...typography.titleMedium,
    color: colors.textPrimary,
    fontWeight: '600',
    flex: 1,
  },
  
  infoRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: spacing.space2,
  },
  
  infoIcon: {
    fontSize: ms(14),
    marginRight: spacing.space2,
  },
  
  infoText: {
    ...typography.bodyMedium,
    color: colors.textSecondary,
  },
  
  footer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: spacing.space3,
  },
  
  statusBadge: {
    paddingHorizontal: spacing.space3,
    paddingVertical: spacing.space1,
    borderRadius: borderRadius.full,
  },
  
  statusBadgeCompleted: {
    backgroundColor: colors.success50,
  },
  
  statusBadgePending: {
    backgroundColor: colors.warning50,
  },
  
  statusBadgeExpired: {
    backgroundColor: colors.neutral100,
  },
  
  statusText: {
    ...typography.labelSmall,
    fontWeight: '600',
  },
  
  chevron: {
    color: colors.neutral300,
  },
};

const fabStyles = {
  container: {
    position: 'absolute',
    bottom: spacing.space6,
    right: layout.screenPadding,
    backgroundColor: colors.brand500,
    borderRadius: borderRadius.full,
    paddingHorizontal: spacing.space5,
    paddingVertical: spacing.space4,
    flexDirection: 'row',
    alignItems: 'center',
    ...shadows.lg,
  },
  
  icon: {
    color: colors.white,
    marginRight: spacing.space2,
  },
  
  text: {
    ...typography.labelMedium,
    color: colors.white,
    fontWeight: '600',
  },
};
```

---

## 2. ContractDetailScreen (계약서 상세)

### 2.1 레이아웃

```
┌─────────────────────────────────────────┐
│ StatusBar                               │
├─────────────────────────────────────────┤
│ Header                                  │
│ ┌─────────────────────────────────────┐ │
│ │ ←   근로계약서               [PDF]  │ │
│ │                           download  │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│                                         │
│  ScrollView                             │
│  paddingHorizontal: layout.screenPadding│
│                                         │
│  Contract Document Card                 │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ │     ━━━━━━━━━━━━━━━━━━━━            │ │
│ │      근  로  계  약  서             │ │
│ │     ━━━━━━━━━━━━━━━━━━━━            │ │
│ │                                     │ │
│ │  사업장: 00카페 강남점              │ │
│ │  근로자: 김직원                     │ │
│ │                                     │ │
│ │  ─────────────────────────          │ │
│ │                                     │ │
│ │  1. 근로 기간                       │ │
│ │     2024.01.01 ~ 2024.12.31        │ │
│ │                                     │ │
│ │  2. 근무 장소                       │ │
│ │     서울시 강남구 ...              │ │
│ │                                     │ │
│ │  3. 업무 내용                       │ │
│ │     카페 서비스 업무               │ │
│ │                                     │ │
│ │  4. 근무 시간                       │ │
│ │     09:00 ~ 18:00 (휴게 1시간)     │ │
│ │                                     │ │
│ │  5. 시급                           │ │
│ │     ₩10,030                        │ │
│ │                                     │ │
│ │  6. 급여 지급일                    │ │
│ │     매월 10일                      │ │
│ │                                     │ │
│ │  backgroundColor: white             │ │
│ │  borderRadius: borderRadius.xl      │ │
│ │  shadows.md                         │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│  marginBottom: spacing.space5          │
│                                         │
│  Signature Section                      │
│ ┌─────────────────────────────────────┐ │
│ │  📝 서명 정보                       │ │
│ │  ─────────────────────────          │ │
│ │                                     │ │
│ │  사업주 서명                        │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │      [서명 이미지]          │    │ │
│ │  │      ✅ 2024.01.01 서명     │    │ │
│ │  └─────────────────────────────┘    │ │
│ │                                     │ │
│ │  근로자 서명                        │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │      [서명 이미지]          │    │ │
│ │  │      ✅ 2024.01.02 서명     │    │ │
│ │  └─────────────────────────────┘    │ │
│ │                                     │ │
│ │  backgroundColor: white             │ │
│ │  borderRadius: borderRadius.xl      │ │
│ │  shadows.sm                         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│  (직원 - 서명 대기 상태일 때만)          │
│  ┌─────────────────────────────────┐    │
│  │      📝 계약서 서명하기         │    │
│  │      PrimaryButton              │    │
│  └─────────────────────────────────┘    │
│                                         │
│  paddingBottom: hp(4) + safeArea        │
│                                         │
└─────────────────────────────────────────┘
```

### 2.2 컴포넌트 스타일

```typescript
const contractDetailStyles = {
  documentCard: {
    backgroundColor: colors.white,
    borderRadius: borderRadius.xl,
    padding: spacing.space6,
    marginTop: spacing.space4,
    marginBottom: spacing.space5,
    ...shadows.md,
  },
  
  documentTitle: {
    ...typography.displaySmall,
    color: colors.textPrimary,
    fontWeight: '700',
    textAlign: 'center',
    marginBottom: spacing.space6,
  },
  
  sectionTitle: {
    ...typography.titleSmall,
    color: colors.brand600,
    fontWeight: '600',
    marginTop: spacing.space4,
    marginBottom: spacing.space2,
  },
  
  sectionContent: {
    ...typography.bodyMedium,
    color: colors.textSecondary,
    lineHeight: ms(24),
  },
  
  signatureCard: {
    backgroundColor: colors.white,
    borderRadius: borderRadius.xl,
    padding: spacing.space5,
    marginBottom: spacing.space5,
    ...shadows.sm,
  },
  
  signatureHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: spacing.space4,
    paddingBottom: spacing.space3,
    borderBottomWidth: 1,
    borderBottomColor: colors.neutral100,
  },
  
  signatureRow: {
    marginBottom: spacing.space4,
  },
  
  signatureLabel: {
    ...typography.labelMedium,
    color: colors.textSecondary,
    marginBottom: spacing.space2,
  },
  
  signatureBox: {
    backgroundColor: colors.neutral50,
    borderRadius: borderRadius.lg,
    padding: spacing.space4,
    alignItems: 'center',
  },
  
  signatureImage: {
    width: '100%',
    height: ms(80),
    resizeMode: 'contain',
  },
  
  signatureDate: {
    ...typography.labelSmall,
    color: colors.success,
    marginTop: spacing.space2,
  },
  
  signaturePending: {
    ...typography.bodyMedium,
    color: colors.textTertiary,
  },
};
```

---

## 3. ContractCreateScreen (계약서 작성)

### 3.1 레이아웃 - 정보 입력

```
┌─────────────────────────────────────────┐
│ StatusBar                               │
├─────────────────────────────────────────┤
│ Header                                  │
│ ┌─────────────────────────────────────┐ │
│ │ ✕   계약서 작성          [미리보기] │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│                                         │
│  ScrollView                             │
│  paddingHorizontal: layout.screenPadding│
│                                         │
│  Info Banner                            │
│ ┌─────────────────────────────────────┐ │
│ │  ℹ️ 입력하신 정보로 대한민국        │ │
│ │     표준 근로계약서가 자동 생성됩니다│ │
│ │  backgroundColor: info50            │ │
│ │  borderRadius: borderRadius.lg      │ │
│ └─────────────────────────────────────┘ │
│  marginBottom: spacing.space5          │
│                                         │
│  Section: 근로자 정보                   │
│ ┌─────────────────────────────────────┐ │
│ │  👤 근로자 정보                     │ │
│ │  ─────────────────────────          │ │
│ │                                     │ │
│ │  근로자 선택 *                      │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ 김직원                    ▼ │    │ │
│ │  │ SelectInput               │    │ │
│ │  └─────────────────────────────┘    │ │
│ │                                     │ │
│ │  backgroundColor: white             │ │
│ │  borderRadius: borderRadius.xl      │ │
│ │  shadows.sm                         │ │
│ └─────────────────────────────────────┘ │
│  marginBottom: spacing.space4          │
│                                         │
│  Section: 계약 기간                     │
│ ┌─────────────────────────────────────┐ │
│ │  📅 계약 기간                       │ │
│ │  ─────────────────────────          │ │
│ │                                     │ │
│ │  근로 기간 *                        │ │
│ │  ┌──────────────┐ ~ ┌──────────────┐│ │
│ │  │ 2024.01.01   │   │ 2024.12.31   ││ │
│ │  │ DatePicker   │   │ DatePicker   ││ │
│ │  └──────────────┘   └──────────────┘│ │
│ │                                     │ │
│ │  backgroundColor: white             │ │
│ └─────────────────────────────────────┘ │
│  marginBottom: spacing.space4          │
│                                         │
│  Section: 근무 조건                     │
│ ┌─────────────────────────────────────┐ │
│ │  💼 근무 조건                       │ │
│ │  ─────────────────────────          │ │
│ │                                     │ │
│ │  근무 장소 *                        │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ 서울시 강남구 테헤란로 123 │    │ │
│ │  │ (사업장 주소 자동 입력)    │    │ │
│ │  └─────────────────────────────┘    │ │
│ │                                     │ │
│ │  업무 내용 *                        │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ 카페 서비스 업무            │    │ │
│ │  └─────────────────────────────┘    │ │
│ │                                     │ │
│ │  근무 시간 *                        │ │
│ │  ┌──────────────┐ ~ ┌──────────────┐│ │
│ │  │ 09:00        │   │ 18:00        ││ │
│ │  └──────────────┘   └──────────────┘│ │
│ │                                     │ │
│ │  휴게 시간 *                        │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ 60분                       ▼│    │ │
│ │  └─────────────────────────────┘    │ │
│ │  ✅ 8시간 근무 시 1시간 휴게 충족   │ │
│ │  color: success                     │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│  marginBottom: spacing.space4          │
│                                         │
│  Section: 급여 조건                     │
│ ┌─────────────────────────────────────┐ │
│ │  💰 급여 조건                       │ │
│ │  ─────────────────────────          │ │
│ │                                     │ │
│ │  시급 *                             │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ ₩ 10,030                   │    │ │
│ │  └─────────────────────────────┘    │ │
│ │  ✅ 2025년 최저시급 ₩10,030 충족    │ │
│ │  color: success                     │ │
│ │                                     │ │
│ │  급여 지급일 *                      │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ 매월 10일                  ▼│    │ │
│ │  └─────────────────────────────┘    │ │
│ │                                     │ │
│ │  급여 지급 방법 *                   │ │
│ │  ┌─────────────────────────────┐    │ │
│ │  │ 근로자 명의 통장에 입금    ▼│    │ │
│ │  └─────────────────────────────┘    │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │   계약서 미리보기 및 서명하기   │    │
│  │   PrimaryButton                 │    │
│  └─────────────────────────────────┘    │
│                                         │
│  paddingBottom: hp(4) + safeArea        │
│                                         │
└─────────────────────────────────────────┘
```

### 3.2 법규 검증 배너

```typescript
const validationStyles = {
  successBanner: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: colors.success50,
    borderRadius: borderRadius.md,
    padding: spacing.space3,
    marginTop: spacing.space2,
  },
  
  successIcon: {
    fontSize: ms(14),
    marginRight: spacing.space2,
  },
  
  successText: {
    ...typography.bodySmall,
    color: colors.success700,
  },
  
  warningBanner: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: colors.warning50,
    borderRadius: borderRadius.md,
    padding: spacing.space3,
    marginTop: spacing.space2,
  },
  
  warningIcon: {
    fontSize: ms(14),
    marginRight: spacing.space2,
  },
  
  warningText: {
    ...typography.bodySmall,
    color: colors.warning700,
  },
  
  errorBanner: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: colors.error50,
    borderRadius: borderRadius.md,
    padding: spacing.space3,
    marginTop: spacing.space2,
  },
  
  errorIcon: {
    fontSize: ms(14),
    marginRight: spacing.space2,
  },
  
  errorText: {
    ...typography.bodySmall,
    color: colors.error700,
  },
};
```

---

## 4. 서명 화면

### 4.1 레이아웃

```
┌─────────────────────────────────────────┐
│ StatusBar                               │
├─────────────────────────────────────────┤
│ Header                                  │
│ ┌─────────────────────────────────────┐ │
│ │                           [✕ 닫기]  │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│                                         │
│  Instruction Text                       │
│ ┌─────────────────────────────────────┐ │
│ │  ✍️ 아래 영역에 서명해주세요        │ │
│ │  typography.titleMedium             │ │
│ │  textAlign: center                  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│  Signature Canvas                       │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ │                                     │ │
│ │                                     │ │
│ │                                     │ │
│ │       (서명 캔버스 영역)            │ │
│ │                                     │ │
│ │       height: hp(35)                │ │
│ │       backgroundColor: white        │ │
│ │       borderWidth: 2                │ │
│ │       borderColor: neutral200       │ │
│ │       borderStyle: dashed           │ │
│ │       borderRadius: borderRadius.lg │ │
│ │                                     │ │
│ │                                     │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                         │
│  Button Row                             │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ │  ┌─────────────┐  ┌─────────────┐   │ │
│ │  │   지우기    │  │    완료     │   │ │
│ │  │   outline   │  │   primary   │   │ │
│ │  └─────────────┘  └─────────────┘   │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

### 4.2 서명 캔버스 컴포넌트

```typescript
const signatureCanvasStyles = {
  container: {
    marginHorizontal: layout.screenPadding,
    marginVertical: spacing.space6,
  },
  
  canvas: {
    height: hp(35),
    backgroundColor: colors.white,
    borderWidth: 2,
    borderColor: colors.neutral200,
    borderStyle: 'dashed',
    borderRadius: borderRadius.lg,
    overflow: 'hidden',
  },
  
  placeholder: {
    position: 'absolute',
    top: '50%',
    left: '50%',
    transform: [{ translateX: -50 }, { translateY: -50 }],
  },
  
  placeholderText: {
    ...typography.bodyMedium,
    color: colors.textTertiary,
  },
  
  buttonRow: {
    flexDirection: 'row',
    gap: spacing.space3,
    marginTop: spacing.space4,
    marginHorizontal: layout.screenPadding,
  },
  
  clearButton: {
    flex: 1,
    paddingVertical: spacing.space4,
    borderRadius: borderRadius.lg,
    borderWidth: 1,
    borderColor: colors.neutral200,
    backgroundColor: colors.white,
    alignItems: 'center',
  },
  
  clearButtonText: {
    ...typography.labelMedium,
    color: colors.textSecondary,
    fontWeight: '600',
  },
  
  confirmButton: {
    flex: 1,
    paddingVertical: spacing.space4,
    borderRadius: borderRadius.lg,
    backgroundColor: colors.brand500,
    alignItems: 'center',
  },
  
  confirmButtonText: {
    ...typography.labelMedium,
    color: colors.white,
    fontWeight: '600',
  },
};
```

---

## 애니메이션

```typescript
const screenAnimations = {
  cardEntry: (index: number) => ({
    entering: FadeInUp.delay(100 + index * 50).duration(400).springify(),
  }),
  
  sectionEntry: (index: number) => ({
    entering: FadeInDown.delay(100 + index * 100).duration(400).springify(),
  }),
  
  fabEntry: {
    entering: ZoomIn.delay(300).duration(400).springify(),
  },
  
  signatureStroke: {
    strokeWidth: withSpring(3, { damping: 15, stiffness: 150 }),
  },
};
```

---

## 접근성

```typescript
const accessibility = {
  contractCard: {
    accessibilityRole: 'button',
    accessibilityLabel: (title, employee, status) =>
      `${title}, ${employee}, ${status}`,
    accessibilityHint: '두 번 탭하여 계약서 상세 보기',
  },
  
  signatureCanvas: {
    accessibilityRole: 'none',
    accessibilityLabel: '서명 영역입니다. 손가락으로 서명해주세요',
  },
  
  pdfButton: {
    accessibilityRole: 'button',
    accessibilityLabel: 'PDF 다운로드',
    accessibilityHint: '두 번 탭하여 계약서 PDF 다운로드',
  },
};
```

---

## 상태 관리

```typescript
interface ContractState {
  // 목록
  contracts: Contract[];
  filter: 'all' | 'pending' | 'completed' | 'expired';
  
  // 작성
  createForm: {
    employeeId: string;
    startDate: Date;
    endDate: Date | null;
    workLocation: string;
    jobDescription: string;
    workStartTime: string;
    workEndTime: string;
    breakMinutes: number;
    hourlyWage: number;
    payDay: number;
    paymentMethod: string;
  };
  
  // 검증
  validation: {
    minWage: boolean;
    workHours: boolean;
    breakTime: boolean;
  };
  
  // 서명
  signatureData: string | null;
  
  // UI
  isLoading: boolean;
  isSubmitting: boolean;
  error: string | null;
}
```

---

## 에러 처리

```typescript
const errorHandling = {
  loadError: {
    title: '계약서를 불러올 수 없습니다',
    message: '네트워크 연결을 확인해주세요',
    action: 'retry',
  },
  
  createError: {
    title: '계약서 생성에 실패했습니다',
    message: '잠시 후 다시 시도해주세요',
    action: 'retry',
  },
  
  signError: {
    title: '서명에 실패했습니다',
    message: '잠시 후 다시 시도해주세요',
    action: 'retry',
  },
  
  pdfError: {
    type: 'toast',
    message: 'PDF 다운로드에 실패했습니다',
  },
  
  validation: {
    minWage: {
      message: '최저시급 이상으로 입력해주세요',
    },
    breakTime: {
      message: '8시간 근무 시 1시간 이상의 휴게시간이 필요합니다',
    },
  },
};
```

---

## 성능 최적화

```typescript
const performanceOptimizations = {
  flatListConfig: {
    initialNumToRender: 5,
    maxToRenderPerBatch: 3,
    windowSize: 5,
    removeClippedSubviews: true,
  },
  
  signatureCanvas: {
    simplifyTolerance: 0.5,
    maxPoints: 1000,
    debounceMs: 16,
  },
  
  pdfGeneration: {
    quality: 0.8,
    maxWidth: 1200,
    compression: true,
  },
  
  memoizedComponents: [
    'ContractCard',
    'ValidationBanner',
    'SignatureCanvas',
  ],
};
```
