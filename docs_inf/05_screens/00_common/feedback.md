# 피드백 컴포넌트 (Feedback Components)

> **Production Ready v2.0** - 프로덕션 수준의 UX/UI 명세

## 개요

사용자에게 상태를 알려주는 피드백 컴포넌트들입니다.

### 🎯 UX 목표
- **즉각적 피드백**: 작업 결과를 즉시 알림
- **명확한 의미**: 유형별 색상/아이콘으로 의미 전달
- **비방해적 표시**: 사용자 흐름을 방해하지 않음

---

## 1. Toast

일시적인 메시지 표시 컴포넌트입니다.

### 1.1 레이아웃

```
┌─────────────────────────────────────────┐
│                                         │
│  ┌─────────────────────────────────────┐│
│  │                                     ││
│  │  ██ ✅ 저장되었습니다.              ││
│  │  ██                                 ││
│  │     Action 버튼 (선택적)            ││
│  │                                     ││
│  │  borderLeftWidth: ms(4)             ││
│  │  borderLeftColor: success           ││
│  │  borderRadius: borderRadius.lg      ││
│  │  backgroundColor: white             ││
│  │  shadows.lg                         ││
│  │                                     ││
│  └─────────────────────────────────────┘│
│                                         │
│  Position:                              │
│  - bottom: hp(12) + safeAreaBottom      │
│  - horizontal: layout.screenPadding    │
│                                         │
└─────────────────────────────────────────┘
```

### 1.2 타입별 스타일

```
┌─────────────────────────────────────────┐
│                                         │
│  Success                                │
│  ┌─────────────────────────────────┐    │
│  │ ██ ✅ 저장되었습니다.           │    │
│  │ ██ borderLeftColor: success     │    │
│  └─────────────────────────────────┘    │
│                                         │
│  Error                                  │
│  ┌─────────────────────────────────┐    │
│  │ ██ ⚠️ 오류가 발생했습니다.      │    │
│  │ ██ borderLeftColor: error       │    │
│  └─────────────────────────────────┘    │
│                                         │
│  Info                                   │
│  ┌─────────────────────────────────┐    │
│  │ ██ ℹ️ 알림 메시지입니다.        │    │
│  │ ██ borderLeftColor: info        │    │
│  └─────────────────────────────────┘    │
│                                         │
│  Warning                                │
│  ┌─────────────────────────────────┐    │
│  │ ██ ⚡ 주의가 필요합니다.         │    │
│  │ ██ borderLeftColor: warning     │    │
│  └─────────────────────────────────┘    │
│                                         │
└─────────────────────────────────────────┘
```

### 1.3 With Action Button

```
┌─────────────────────────────────────────┐
│                                         │
│  ┌─────────────────────────────────────┐│
│  │ ██                                  ││
│  │ ██ ✅ 항목이 삭제되었습니다.   [실행취소]
│  │ ██                                  ││
│  └─────────────────────────────────────┘│
│                                         │
│  Action Button:                         │
│  - typography.labelMedium               │
│  - color: brand500                      │
│  - fontWeight: 600                      │
│  - paddingLeft: spacing.space3          │
│                                         │
└─────────────────────────────────────────┘
```

### 1.4 컴포넌트 스타일

```typescript
const toastStyles = {
  container: {
    position: 'absolute',
    bottom: hp(12),
    left: layout.screenPadding,
    right: layout.screenPadding,
    zIndex: 9999,
  },
  
  toast: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: colors.white,
    borderRadius: borderRadius.lg,
    borderLeftWidth: ms(4),
    paddingHorizontal: spacing.space4,
    paddingVertical: spacing.space4,
    minHeight: ms(56),
    ...shadows.lg,
  },
  
  icon: {
    fontSize: ms(20),
    marginRight: spacing.space3,
  },
  
  content: {
    flex: 1,
  },
  
  message: {
    ...typography.bodyMedium,
    color: colors.textPrimary,
  },
  
  action: {
    ...typography.labelMedium,
    color: colors.brand500,
    fontWeight: '600',
    paddingLeft: spacing.space3,
  },
};

const toastTypeStyles = {
  success: {
    borderLeftColor: colors.success,
    iconColor: colors.success,
  },
  error: {
    borderLeftColor: colors.error,
    iconColor: colors.error,
  },
  info: {
    borderLeftColor: colors.info,
    iconColor: colors.info,
  },
  warning: {
    borderLeftColor: colors.warning,
    iconColor: colors.warning,
  },
};

const toastIcons = {
  success: '✅',
  error: '⚠️',
  info: 'ℹ️',
  warning: '⚡',
};
```

### 1.5 애니메이션

```typescript
const toastAnimations = {
  // 등장
  enter: {
    translateY: withSpring(0, {
      damping: 15,
      stiffness: 150,
    }),
    opacity: withTiming(1, { duration: 200 }),
  },
  
  // 퇴장
  exit: {
    translateY: withTiming(hp(10), { duration: 200 }),
    opacity: withTiming(0, { duration: 150 }),
  },
  
  // 초기 상태
  initial: {
    translateY: hp(10),
    opacity: 0,
  },
};
```

### 1.6 Props

```typescript
interface ToastProps {
  message: string;
  type: 'success' | 'error' | 'info' | 'warning';
  duration?: number;        // default: 3000ms
  onDismiss?: () => void;
  action?: {
    label: string;
    onPress: () => void;
  };
  position?: 'top' | 'bottom';  // default: 'bottom'
}
```

---

## 2. Loading Indicator

로딩 상태 표시 컴포넌트입니다.

### 2.1 Full Screen Loading

```
┌─────────────────────────────────────────┐
│                                         │
│                                         │
│                                         │
│                                         │
│              ○ ○ ○                      │
│            (spinner)                    │
│                                         │
│           로딩 중...                    │
│                                         │
│                                         │
│                                         │
│                                         │
│  backgroundColor: rgba(255,255,255,0.95)│
│  position: absolute, full screen        │
│  justifyContent: center                 │
│  alignItems: center                     │
│                                         │
└─────────────────────────────────────────┘
```

### 2.2 Inline Loading

```
┌─────────────────────────────────────────┐
│                                         │
│  Small (버튼 내부용)                    │
│  ┌───────────────────────┐              │
│  │  ○○○  처리 중...      │              │
│  │  size: ms(16)         │              │
│  └───────────────────────┘              │
│                                         │
│  Medium (일반)                          │
│  ┌───────────────────────┐              │
│  │       ○○○             │              │
│  │    size: ms(24)       │              │
│  └───────────────────────┘              │
│                                         │
│  Large (페이지 로딩)                    │
│  ┌───────────────────────┐              │
│  │       ○○○             │              │
│  │    size: ms(40)       │              │
│  │                       │              │
│  │    로딩 중...         │              │
│  └───────────────────────┘              │
│                                         │
└─────────────────────────────────────────┘
```

### 2.3 Skeleton Loading

```
┌─────────────────────────────────────────┐
│                                         │
│  Card Skeleton                          │
│  ┌─────────────────────────────────┐    │
│  │ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │    │
│  │ ░░░░░░░░░░░░░░░░░░░            │    │
│  │ ░░░░░░░░░░░░░░░░░░░░░░░░░░     │    │
│  └─────────────────────────────────┘    │
│                                         │
│  List Item Skeleton                     │
│  ┌─────────────────────────────────┐    │
│  │ ┌────┐ ░░░░░░░░░░░░░           │    │
│  │ │░░░░│ ░░░░░░░░░░              │    │
│  │ └────┘                          │    │
│  └─────────────────────────────────┘    │
│                                         │
│  Shimmer Animation:                     │
│  - backgroundColor: neutral100          │
│  - shimmerColor: neutral50              │
│  - duration: 1500ms                     │
│  - infinite                             │
│                                         │
└─────────────────────────────────────────┘
```

### 2.4 컴포넌트 스타일

```typescript
const loadingStyles = {
  // Full Screen
  fullScreenOverlay: {
    ...StyleSheet.absoluteFillObject,
    backgroundColor: 'rgba(255, 255, 255, 0.95)',
    justifyContent: 'center',
    alignItems: 'center',
    zIndex: 9999,
  },
  
  spinnerLarge: {
    width: ms(40),
    height: ms(40),
  },
  
  loadingText: {
    ...typography.bodyMedium,
    color: colors.textSecondary,
    marginTop: spacing.space3,
  },
  
  // Inline
  inlineContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: spacing.space2,
  },
  
  spinnerSmall: { width: ms(16), height: ms(16) },
  spinnerMedium: { width: ms(24), height: ms(24) },
  
  // Skeleton
  skeleton: {
    backgroundColor: colors.neutral100,
    borderRadius: borderRadius.md,
    overflow: 'hidden',
  },
  
  shimmer: {
    width: '100%',
    height: '100%',
  },
};

const spinnerSizes = {
  small: ms(16),
  medium: ms(24),
  large: ms(40),
};
```

### 2.5 Props

```typescript
interface LoadingIndicatorProps {
  visible?: boolean;
  fullScreen?: boolean;
  message?: string;
  size?: 'small' | 'medium' | 'large';
  color?: string;
}

interface SkeletonProps {
  width?: number | string;
  height?: number | string;
  borderRadius?: number;
  style?: ViewStyle;
}
```

---

## 3. Empty State

데이터가 없을 때 표시하는 컴포넌트입니다.

### 3.1 레이아웃

```
┌─────────────────────────────────────────┐
│                                         │
│  중앙 정렬 레이아웃                     │
│  flex: 1                                │
│  justifyContent: center                 │
│  alignItems: center                     │
│  paddingHorizontal: layout.screenPadding│
│                                         │
│              📋                         │
│          fontSize: ms(64)               │
│                                         │
│     데이터가 없습니다                   │
│     typography.titleMedium              │
│     color: textSecondary                │
│     marginTop: spacing.space4           │
│                                         │
│   새로운 항목을 추가해보세요.           │
│   typography.bodyMedium                 │
│   color: textTertiary                   │
│   marginTop: spacing.space2             │
│                                         │
│   ┌─────────────────────────┐           │
│   │    + 항목 추가하기      │           │
│   │    PrimaryButton        │           │
│   └─────────────────────────┘           │
│   marginTop: spacing.space6             │
│                                         │
└─────────────────────────────────────────┘
```

### 3.2 Variants

```
┌─────────────────────────────────────────┐
│                                         │
│  검색 결과 없음                         │
│  ┌─────────────────────────────────┐    │
│  │            🔍                   │    │
│  │                                 │    │
│  │    검색 결과가 없습니다         │    │
│  │    다른 키워드로 검색해보세요   │    │
│  └─────────────────────────────────┘    │
│                                         │
│  오류 발생                              │
│  ┌─────────────────────────────────┐    │
│  │            ⚠️                   │    │
│  │                                 │    │
│  │    문제가 발생했습니다          │    │
│  │    잠시 후 다시 시도해주세요    │    │
│  │                                 │    │
│  │      [다시 시도]                │    │
│  └─────────────────────────────────┘    │
│                                         │
│  네트워크 오류                          │
│  ┌─────────────────────────────────┐    │
│  │            📡                   │    │
│  │                                 │    │
│  │  인터넷 연결을 확인해주세요     │    │
│  │                                 │    │
│  │      [새로고침]                 │    │
│  └─────────────────────────────────┘    │
│                                         │
└─────────────────────────────────────────┘
```

### 3.3 컴포넌트 스타일

```typescript
const emptyStateStyles = {
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: layout.screenPadding,
    paddingVertical: spacing.space10,
  },
  
  icon: {
    fontSize: ms(64),
    marginBottom: spacing.space4,
  },
  
  title: {
    ...typography.titleMedium,
    color: colors.textSecondary,
    textAlign: 'center',
    marginBottom: spacing.space2,
  },
  
  message: {
    ...typography.bodyMedium,
    color: colors.textTertiary,
    textAlign: 'center',
    lineHeight: ms(22),
    marginBottom: spacing.space6,
  },
  
  actionButton: {
    minWidth: wp(50),
  },
};

const emptyStatePresets = {
  noData: {
    icon: '📋',
    title: '데이터가 없습니다',
    message: '새로운 항목을 추가해보세요',
  },
  noSearch: {
    icon: '🔍',
    title: '검색 결과가 없습니다',
    message: '다른 키워드로 검색해보세요',
  },
  error: {
    icon: '⚠️',
    title: '문제가 발생했습니다',
    message: '잠시 후 다시 시도해주세요',
  },
  network: {
    icon: '📡',
    title: '연결할 수 없습니다',
    message: '인터넷 연결을 확인해주세요',
  },
};
```

### 3.4 Props

```typescript
interface EmptyStateProps {
  icon?: string | ReactNode;
  title: string;
  message?: string;
  action?: {
    label: string;
    onPress: () => void;
  };
  preset?: 'noData' | 'noSearch' | 'error' | 'network';
}
```

---

## 4. Progress Indicator

진행 상태 표시 컴포넌트입니다.

### 4.1 Linear Progress

```
┌─────────────────────────────────────────┐
│                                         │
│  Determinate (확정)                     │
│  ┌─────────────────────────────────┐    │
│  │ [████████████░░░░░░░░░░░░░░░░░░] 65%  │    │
│  └─────────────────────────────────┘    │
│                                         │
│  Indeterminate (불확정)                 │
│  ┌─────────────────────────────────┐    │
│  │ [░░░░████░░░░░░░░░░░░░░░░░░░]  │    │
│  │      (moving animation)         │    │
│  └─────────────────────────────────┘    │
│                                         │
│  스타일 명세:                           │
│  - height: ms(4)                        │
│  - borderRadius: borderRadius.full      │
│  - trackColor: neutral100               │
│  - progressColor: brand500              │
│                                         │
└─────────────────────────────────────────┘
```

### 4.2 Circular Progress

```
┌─────────────────────────────────────────┐
│                                         │
│     ┌─────┐                             │
│     │ ╭─╮ │  size: ms(48)               │
│     │ │65│ │  strokeWidth: ms(4)        │
│     │ ╰─╯ │  color: brand500            │
│     └─────┘                             │
│                                         │
│  With Label                             │
│     ┌─────┐                             │
│     │ ╭─╮ │                             │
│     │ │65│ │  65%                       │
│     │ │% │ │  typography.titleSmall     │
│     │ ╰─╯ │                             │
│     └─────┘                             │
│                                         │
└─────────────────────────────────────────┘
```

### 4.3 컴포넌트 스타일

```typescript
const progressStyles = {
  // Linear Progress
  linearTrack: {
    height: ms(4),
    backgroundColor: colors.neutral100,
    borderRadius: borderRadius.full,
    overflow: 'hidden',
  },
  
  linearProgress: {
    height: '100%',
    backgroundColor: colors.brand500,
    borderRadius: borderRadius.full,
  },
  
  // Circular Progress
  circularContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  
  circularLabel: {
    position: 'absolute',
    ...typography.titleSmall,
    color: colors.textPrimary,
    fontWeight: '600',
  },
};
```

---

## 접근성

```typescript
const accessibility = {
  toast: {
    accessibilityRole: 'alert',
    accessibilityLiveRegion: 'polite',
    accessibilityLabel: (type: string, message: string) =>
      `${type}: ${message}`,
  },
  
  loading: {
    accessibilityRole: 'progressbar',
    accessibilityLabel: '로딩 중',
    accessibilityState: { busy: true },
  },
  
  emptyState: {
    accessibilityRole: 'text',
    accessibilityLabel: (title: string, message?: string) =>
      message ? `${title}. ${message}` : title,
  },
  
  progress: {
    accessibilityRole: 'progressbar',
    accessibilityValue: { now: progress, min: 0, max: 100 },
    accessibilityLabel: (progress: number) => `${progress}% 완료`,
  },
};
```

---

## 사용 예시

```tsx
// Toast
showToast({
  type: 'success',
  message: '저장되었습니다.',
  duration: 3000,
});

showToast({
  type: 'error',
  message: '삭제되었습니다.',
  action: {
    label: '실행취소',
    onPress: handleUndo,
  },
});

// Loading
<LoadingIndicator fullScreen message="로딩 중..." />
<LoadingIndicator size="small" />

// Empty State
<EmptyState preset="noData" action={{ label: '추가하기', onPress: handleAdd }} />
<EmptyState
  icon="🔍"
  title="검색 결과가 없습니다"
  message="다른 키워드로 검색해보세요"
/>

// Progress
<LinearProgress progress={65} />
<CircularProgress progress={65} showLabel />
```
