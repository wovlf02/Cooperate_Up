# 카드 컴포넌트 (Card Components) - Production Ready v2.0

## 개요

프로덕션급 UX를 위한 카드 컴포넌트입니다.
부드러운 그림자, 마이크로 인터랙션, 스켈레톤 로딩 등 세련된 사용자 경험을 제공합니다.

---

## 🎨 디자인 원칙

### 카드 계층 구조

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Elevated Card    ▓▓▓▓▓▓▓  그림자로 떠있는 카드             │
│                                                             │
│  Filled Card      ████████  배경색으로 구분되는 카드        │
│                                                             │
│  Outlined Card    ░░░░░░░░  테두리로 구분되는 카드          │
│                                                             │
│  Glass Card       ▒▒▒▒▒▒▒▒  블러 효과의 투명 카드          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 인터랙션 원칙

- **부드러운 터치 반응**: 터치 시 미세한 스케일 축소 (0.98-0.99)
- **그림자 변화**: 누를 때 그림자 감소, 떼면 원복
- **스프링 애니메이션**: 자연스러운 바운스 효과
- **명확한 터치 영역**: 전체 카드가 터치 가능

---

## 1. Base Card

기본 카드 컴포넌트입니다.

### 레이아웃

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │   Card Content                                      │  │
│  │                                                     │  │
│  │   padding: 20px (space5)                            │  │
│  │   borderRadius: 16px (lg)                           │  │
│  │   backgroundColor: surfaceElevated (#FFFFFF)        │  │
│  │                                                     │  │
│  │   ⚡ Soft Shadow (sm)                                │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
│  margin: screenPadding (좌우)                             │
│  marginBottom: 16px                                       │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 스타일 스펙

| 속성 | 값 | 비고 |
|------|-----|------|
| padding | `ms(20)` | space5 |
| borderRadius | `ms(16)` | borderRadius.lg |
| backgroundColor | `surfaceElevated` | #FFFFFF |
| marginHorizontal | `wp(5)` | screenPadding |
| marginBottom | `ms(16)` | space4 |

### 그림자 (Elevation)

```typescript
// iOS
shadowColor: '#000',
shadowOffset: { width: 0, height: 1 },
shadowOpacity: 0.08,
shadowRadius: 8,

// Android
elevation: 2,
```

### 카드 변형

```typescript
const cardVariants = {
  // Elevated (기본, 그림자)
  elevated: {
    backgroundColor: colors.surfaceElevated,
    ...shadows.sm,
  },
  
  // Filled (배경색)
  filled: {
    backgroundColor: colors.neutral100,
    borderWidth: 0,
  },
  
  // Outlined (테두리)
  outlined: {
    backgroundColor: colors.surfaceElevated,
    borderWidth: 1,
    borderColor: colors.border,
  },
  
  // Glass (블러)
  glass: {
    backgroundColor: 'rgba(255, 255, 255, 0.8)',
    // iOS에서 BlurView 사용
  },
};
```

### Props Interface

```typescript
interface CardProps {
  children: ReactNode;
  
  // 스타일
  variant?: 'elevated' | 'filled' | 'outlined' | 'glass';
  padding?: 'none' | 'small' | 'medium' | 'large';
  
  // 인터랙션
  onPress?: () => void;
  disabled?: boolean;
  
  // 커스텀
  style?: ViewStyle;
  
  // 접근성
  accessibilityLabel?: string;
}
```

---

## 2. Interactive Card

터치 가능한 인터랙티브 카드입니다.

### 애니메이션

```typescript
// 프레스 애니메이션
const pressAnimation = {
  scale: {
    from: 1,
    to: 0.98,
    duration: 100,
    easing: 'easeOut',
  },
  shadow: {
    from: shadows.sm,
    to: shadows.xs,
    duration: 100,
  },
};

// 릴리즈 애니메이션 (스프링)
const releaseAnimation = {
  type: 'spring',
  damping: 15,
  stiffness: 200,
  mass: 0.8,
};
```

### 리플 효과 (Android)

```typescript
// Android Ripple Config
const rippleConfig = {
  color: colors.brand100,
  borderless: false,
  radius: undefined, // 자동 계산
};
```

---

## 3. Feature Card (기능 카드)

메인 기능을 강조하는 대형 카드입니다.

### 레이아웃

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  ╔════════════════════════════════════════════════╗ │  │
│  │  ║                                                ║ │  │
│  │  ║  ┌────┐                                        ║ │  │
│  │  ║  │ 📊 │  매출 분석                             ║ │  │
│  │  ║  └────┘  실시간 매출 현황을 확인하세요         ║ │  │
│  │  ║                                                ║ │  │
│  │  ║  ┌───────────────────────────────────────────┐ ║ │  │
│  │  ║  │                                           │ ║ │  │
│  │  ║  │         [ 그래프 / 일러스트 ]             │ ║ │  │
│  │  ║  │                                           │ ║ │  │
│  │  ║  └───────────────────────────────────────────┘ ║ │  │
│  │  ║                                                ║ │  │
│  │  ║                         바로가기 →            ║ │  │
│  │  ║                                                ║ │  │
│  │  ╚════════════════════════════════════════════════╝ │  │
│  │                                                     │  │
│  │  borderRadius: 20px (xl)                            │  │
│  │  padding: 24px                                      │  │
│  │  minHeight: 180px                                   │  │
│  │                                                     │  │
│  │  Gradient Background (optional)                     │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 스타일 스펙

```typescript
const featureCardStyles = {
  container: {
    borderRadius: borderRadius.xl,
    padding: space6,
    minHeight: ms(180),
    overflow: 'hidden',
  },
  
  // 그라데이션 배경 옵션
  gradientBackground: {
    colors: [colors.brand500, colors.brand600],
    start: { x: 0, y: 0 },
    end: { x: 1, y: 1 },
  },
  
  icon: {
    size: ms(40),
    backgroundColor: 'rgba(255, 255, 255, 0.2)',
    borderRadius: borderRadius.md,
    padding: space3,
  },
  
  title: {
    ...typography.headlineMedium,
    color: colors.white,
    marginTop: space3,
  },
  
  description: {
    ...typography.bodyMedium,
    color: 'rgba(255, 255, 255, 0.8)',
    marginTop: space1,
  },
  
  action: {
    ...typography.labelLarge,
    color: colors.white,
    marginTop: 'auto',
  },
};
```

---

## 4. Stat Card (통계 카드)

숫자/통계를 강조하는 카드입니다.

### 단일 통계 카드

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  💰                                                 │  │
│  │                                                     │  │
│  │  이번 달 급여                                       │  │
│  │  fontSize: 14, color: neutral500                    │  │
│  │                                                     │  │
│  │  ₩1,534,590                                        │  │
│  │  fontSize: 32, fontWeight: 700                      │  │
│  │  color: brand500                                    │  │
│  │                                                     │  │
│  │  ↑ 12.5% 지난달 대비                               │  │
│  │  fontSize: 13, color: success                       │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 다중 통계 카드 (Grid)

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  📊 이번 달 현황                                    │  │
│  │  ─────────────────────────────────────────────────  │  │
│  │                                                     │  │
│  │  ┌───────────────┬───────────────┬───────────────┐  │  │
│  │  │               │               │               │  │  │
│  │  │   출근일      │   근무시간    │   평균급여    │  │  │
│  │  │   18일        │   136h        │   ₩85,255    │  │  │
│  │  │               │               │               │  │  │
│  │  └───────────────┴───────────────┴───────────────┘  │  │
│  │                                                     │  │
│  │  flex: 1 each                                       │  │
│  │  구분선: 1px, neutral200                            │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 스타일 스펙

```typescript
const statCardStyles = {
  container: {
    backgroundColor: colors.surfaceElevated,
    borderRadius: borderRadius.lg,
    padding: space5,
    ...shadows.sm,
  },
  
  icon: {
    size: ms(28),
    marginBottom: space3,
  },
  
  label: {
    ...typography.bodySmall,
    color: colors.textSecondary,
    marginBottom: space1,
  },
  
  value: {
    ...typography.displayLarge,
    color: colors.textPrimary,
    // 숫자 폰트는 tabular (고정폭)
    fontVariant: ['tabular-nums'],
  },
  
  // 변화량 표시
  change: {
    positive: {
      ...typography.labelMedium,
      color: colors.success,
    },
    negative: {
      ...typography.labelMedium,
      color: colors.error,
    },
    neutral: {
      ...typography.labelMedium,
      color: colors.textTertiary,
    },
  },
  
  // 구분선 (다중 통계)
  divider: {
    width: 1,
    backgroundColor: colors.border,
    marginHorizontal: space3,
  },
};
```

### 애니메이션

```typescript
// 숫자 카운트업 애니메이션
const countUpAnimation = {
  from: 0,
  to: finalValue,
  duration: 1000,
  easing: 'easeOut',
  delay: 200, // 카드 진입 후 시작
};

// 변화량 뱃지 팝인
const changePopAnimation = {
  from: { scale: 0, opacity: 0 },
  to: { scale: 1, opacity: 1 },
  delay: 800,
  type: 'spring',
};
```

---

## 5. List Item Card

목록 아이템용 카드입니다.

### 레이아웃

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  ┌────┐  제목 텍스트                         ┌───┐  │  │
│  │  │    │  설명 텍스트 (한 줄)                 │ > │  │  │
│  │  │ 🏢 │                                      │   │  │  │
│  │  │    │  ┌─────┐                             └───┘  │  │
│  │  └────┘  │ NEW │  (Optional Badge)                  │  │
│  │          └─────┘                                    │  │
│  │                                                     │  │
│  │  height: 72px (minHeight)                           │  │
│  │  paddingHorizontal: 16px                            │  │
│  │  paddingVertical: 14px                              │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
│  └ 아래 구분선 또는 카드 간격                             │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 스타일 스펙

```typescript
const listItemCardStyles = {
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    minHeight: ms(72),
    paddingHorizontal: space4,
    paddingVertical: space3,
    backgroundColor: colors.surfaceElevated,
  },
  
  // 왼쪽 아이콘 컨테이너
  iconContainer: {
    width: ms(44),
    height: ms(44),
    borderRadius: borderRadius.md,
    backgroundColor: colors.brand100,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: space3,
  },
  
  icon: {
    size: ms(24),
    color: colors.brand600,
  },
  
  // 콘텐츠 영역
  content: {
    flex: 1,
  },
  
  title: {
    ...typography.titleMedium,
    color: colors.textPrimary,
  },
  
  subtitle: {
    ...typography.bodySmall,
    color: colors.textSecondary,
    marginTop: ms(2),
  },
  
  // 오른쪽 요소
  rightElement: {
    marginLeft: space3,
  },
  
  chevron: {
    size: ms(20),
    color: colors.neutral400,
  },
  
  // 구분선
  separator: {
    height: 1,
    backgroundColor: colors.borderLight,
    marginLeft: ms(60), // 아이콘 너비 + 간격
  },
};
```

### 변형

```typescript
const listItemVariants = {
  // 기본
  default: {
    showChevron: true,
    showSeparator: true,
  },
  
  // 컴팩트
  compact: {
    minHeight: ms(56),
    paddingVertical: space2,
  },
  
  // 확장 (추가 정보 포함)
  expanded: {
    minHeight: ms(88),
    // 두 줄 설명 가능
  },
  
  // 스와이프 가능
  swipeable: {
    // 좌우 스와이프 액션
    leftActions: [{ icon: 'edit', color: colors.brand500 }],
    rightActions: [{ icon: 'trash', color: colors.error }],
  },
};
```

---

## 6. Action Card

액션 버튼이 포함된 카드입니다.

### 레이아웃

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  🔔 새로운 알림이 있습니다                          │  │
│  │     headlineMedium, textPrimary                     │  │
│  │                                                     │  │
│  │  체크리스트 3개가 미완료 상태입니다.                │  │
│  │  퇴근 전에 완료해주세요.                            │  │
│  │     bodyMedium, textSecondary                       │  │
│  │                                                     │  │
│  │  ─────────────────────────────────────────────────  │  │
│  │                                                     │  │
│  │  ┌─────────────────┐  ┌─────────────────┐          │  │
│  │  │     나중에      │  │    확인하기     │          │  │
│  │  │   (Ghost)       │  │   (Primary)     │          │  │
│  │  └─────────────────┘  └─────────────────┘          │  │
│  │                                                     │  │
│  │  gap: 12px                                          │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 스타일 스펙

```typescript
const actionCardStyles = {
  container: {
    backgroundColor: colors.surfaceElevated,
    borderRadius: borderRadius.lg,
    padding: space5,
    ...shadows.sm,
  },
  
  header: {
    flexDirection: 'row',
    alignItems: 'flex-start',
  },
  
  icon: {
    size: ms(24),
    marginRight: space3,
    color: colors.brand500,
  },
  
  title: {
    ...typography.headlineSmall,
    color: colors.textPrimary,
    flex: 1,
  },
  
  description: {
    ...typography.bodyMedium,
    color: colors.textSecondary,
    marginTop: space2,
    lineHeight: fs(22),
  },
  
  divider: {
    height: 1,
    backgroundColor: colors.borderLight,
    marginVertical: space4,
  },
  
  actions: {
    flexDirection: 'row',
    justifyContent: 'flex-end',
    gap: space3,
  },
};
```

---

## 7. Summary Card

요약 정보 카드 (급여, 통계 등)입니다.

### 레이아웃

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  ╔═══════════════════════════════════════════════╗  │  │
│  │  ║                                               ║  │  │
│  │  ║  💰 12월 예상 급여                            ║  │  │
│  │  ║     labelLarge, textSecondary                 ║  │  │
│  │  ║                                               ║  │  │
│  │  ║            ₩1,534,590                        ║  │  │
│  │  ║     displayLarge, brand500                    ║  │  │
│  │  ║     fontVariant: tabular-nums                 ║  │  │
│  │  ║                                               ║  │  │
│  │  ║  18일 근무 • 136시간                          ║  │  │
│  │  ║     bodySmall, textTertiary                   ║  │  │
│  │  ║                                               ║  │  │
│  │  ║  ─────────────────────────────────────────    ║  │  │
│  │  ║                                               ║  │  │
│  │  ║  상세보기                              →      ║  │  │
│  │  ║     labelLarge, brand500                      ║  │  │
│  │  ║                                               ║  │  │
│  │  ╚═══════════════════════════════════════════════╝  │  │
│  │                                                     │  │
│  │  Gradient Border or Highlight                       │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 강조 스타일 (Highlighted)

```typescript
const highlightedSummaryCard = {
  container: {
    backgroundColor: colors.brand50,
    borderWidth: 2,
    borderColor: colors.brand500,
    borderRadius: borderRadius.lg,
    padding: space5,
  },
  
  // 또는 그라데이션 테두리
  gradientBorder: {
    borderWidth: 2,
    borderColor: 'transparent',
    // LinearGradient as border
  },
};
```

---

## 8. Expandable Card

펼침/접힘이 가능한 카드입니다.

### 접힌 상태

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  📋 근무 상세 내역                          ▼      │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 펼친 상태

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  📋 근무 상세 내역                          ▲      │  │
│  │                                                     │  │
│  │  ─────────────────────────────────────────────────  │  │
│  │                                                     │  │
│  │  12/20 (금)    09:00 - 18:00    8h    ₩85,000     │  │
│  │  12/19 (목)    09:00 - 18:00    8h    ₩85,000     │  │
│  │  12/18 (수)    09:00 - 18:00    8h    ₩85,000     │  │
│  │  ...                                                │  │
│  │                                                     │  │
│  │  전체 보기 →                                        │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 애니메이션

```typescript
// 펼침/접힘 애니메이션
const expandAnimation = {
  height: {
    // LayoutAnimation or Reanimated
    duration: 300,
    easing: 'easeInOut',
  },
  
  // 화살표 회전
  chevron: {
    from: { rotate: '0deg' },
    to: { rotate: '180deg' },
    duration: 200,
  },
  
  // 콘텐츠 페이드
  content: {
    from: { opacity: 0, translateY: -10 },
    to: { opacity: 1, translateY: 0 },
    duration: 200,
    delay: 100,
  },
};
```

---

## 9. Progress Card

진행률 표시 카드입니다.

### 레이아웃

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  ✅ 오늘 체크리스트                          68%   │  │
│  │     headlineSmall                      displaySmall │  │
│  │                                                     │  │
│  │  ┌───────────────────────────────────────────────┐  │  │
│  │  │██████████████████████████░░░░░░░░░░░░░░░░░░░░│  │  │
│  │  └───────────────────────────────────────────────┘  │  │
│  │                                                     │  │
│  │  height: 8px                                        │  │
│  │  borderRadius: 4px                                  │  │
│  │  backgroundColor: neutral200 (track)                │  │
│  │  fillColor: brand500 or gradient                    │  │
│  │                                                     │  │
│  │  15/22 항목 완료                                    │  │
│  │     bodySmall, textSecondary                        │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 진행률 바 스타일

```typescript
const progressBarStyles = {
  track: {
    height: ms(8),
    borderRadius: ms(4),
    backgroundColor: colors.neutral200,
    overflow: 'hidden',
  },
  
  fill: {
    height: '100%',
    borderRadius: ms(4),
    // 그라데이션 옵션
    // backgroundColor: colors.brand500,
  },
  
  // 그라데이션 채우기
  gradientFill: {
    colors: [colors.brand400, colors.brand600],
    start: { x: 0, y: 0 },
    end: { x: 1, y: 0 },
  },
  
  // 애니메이션
  animation: {
    duration: 800,
    easing: 'easeOut',
    delay: 200,
  },
};
```

### 원형 진행률

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│         ┌─────────────────┐                               │
│         │                 │                               │
│         │    ┌─────┐      │                               │
│         │   ╱       ╲     │      CircularProgress         │
│         │  │   68%  │     │      - size: 80px             │
│         │   ╲       ╱     │      - strokeWidth: 8px       │
│         │    └─────┘      │      - trackColor: neutral200 │
│         │                 │      - fillColor: brand500    │
│         └─────────────────┘                               │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## 10. Quick Action Card

빠른 액션 그리드 카드입니다.

### 레이아웃 (2x2 Grid)

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  ┌───────────────┐  ┌───────────────┐              │  │
│  │  │               │  │               │              │  │
│  │  │      ⏰       │  │      📋       │              │  │
│  │  │    출근하기   │  │   체크리스트  │              │  │
│  │  │               │  │               │              │  │
│  │  └───────────────┘  └───────────────┘              │  │
│  │                                                     │  │
│  │  ┌───────────────┐  ┌───────────────┐              │  │
│  │  │               │  │               │              │  │
│  │  │      📅       │  │      💬       │              │  │
│  │  │    캘린더     │  │     채팅      │              │  │
│  │  │               │  │               │              │  │
│  │  └───────────────┘  └───────────────┘              │  │
│  │                                                     │  │
│  │  gap: 12px                                          │  │
│  │  각 아이템 aspectRatio: 1 (정사각형)                │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 스타일 스펙

```typescript
const quickActionCardStyles = {
  grid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: space3,
    padding: space4,
  },
  
  item: {
    flex: 1,
    minWidth: '45%',
    aspectRatio: 1,
    backgroundColor: colors.neutral100,
    borderRadius: borderRadius.lg,
    justifyContent: 'center',
    alignItems: 'center',
    padding: space4,
  },
  
  itemPressed: {
    backgroundColor: colors.brand100,
    transform: [{ scale: 0.96 }],
  },
  
  icon: {
    size: ms(32),
    color: colors.brand500,
    marginBottom: space2,
  },
  
  label: {
    ...typography.labelLarge,
    color: colors.textPrimary,
    textAlign: 'center',
  },
};
```

---

## 11. 스켈레톤 로딩

카드 로딩 상태입니다.

### 레이아웃

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  ┌────┐  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓          │  │
│  │  │▓▓▓▓│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓                       │  │
│  │  └────┘                                             │  │
│  │                                                     │  │
│  │  Shimmer Animation                                  │  │
│  │  - 좌에서 우로 빛 이동                              │  │
│  │  - duration: 1.5s                                   │  │
│  │  - repeat: infinite                                 │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

### 스켈레톤 스타일

```typescript
const skeletonStyles = {
  base: {
    backgroundColor: colors.neutral200,
    borderRadius: borderRadius.sm,
    overflow: 'hidden',
  },
  
  // 쉬머 애니메이션
  shimmer: {
    // LinearGradient 이동
    colors: [
      colors.neutral200,
      colors.neutral100,
      colors.neutral200,
    ],
    start: { x: 0, y: 0 },
    end: { x: 1, y: 0 },
    animation: {
      translateX: {
        from: -100,
        to: 100,
        duration: 1500,
        repeat: -1,
      },
    },
  },
  
  // 변형
  variants: {
    text: {
      height: ms(16),
      width: '80%',
    },
    textShort: {
      height: ms(14),
      width: '40%',
    },
    avatar: {
      width: ms(44),
      height: ms(44),
      borderRadius: borderRadius.full,
    },
    image: {
      width: '100%',
      height: ms(120),
    },
  },
};
```

---

## 12. 접근성

### 요구사항

```typescript
const cardAccessibility = {
  // 터치 가능한 카드
  interactive: {
    accessibilityRole: 'button',
    accessibilityLabel: '카드 제목 및 설명',
    accessibilityHint: '두 번 탭하여 상세 보기',
    accessibilityState: {
      disabled: false,
    },
  },
  
  // 정보 표시 카드
  informative: {
    accessibilityRole: 'text',
    accessible: true,
    // 모든 내용이 하나로 읽힘
  },
  
  // 확장 가능 카드
  expandable: {
    accessibilityRole: 'button',
    accessibilityState: {
      expanded: boolean,
    },
    accessibilityHint: '두 번 탭하여 펼치기/접기',
  },
};
```

---

## 13. 전체 코드 예시

```typescript
// components/Card/Card.tsx

import React from 'react';
import { Pressable, View, StyleSheet } from 'react-native';
import Animated, {
  useAnimatedStyle,
  useSharedValue,
  withSpring,
  withTiming,
} from 'react-native-reanimated';
import { colors, shadows, borderRadius, spacing } from '@/styles/theme';
import { ms } from '@/utils/responsive';

const AnimatedPressable = Animated.createAnimatedComponent(Pressable);

interface CardProps {
  children: React.ReactNode;
  variant?: 'elevated' | 'filled' | 'outlined';
  padding?: 'none' | 'small' | 'medium' | 'large';
  onPress?: () => void;
  style?: ViewStyle;
}

export const Card: React.FC<CardProps> = ({
  children,
  variant = 'elevated',
  padding = 'medium',
  onPress,
  style,
}) => {
  const scale = useSharedValue(1);
  
  const animatedStyle = useAnimatedStyle(() => ({
    transform: [{ scale: scale.value }],
  }));
  
  const handlePressIn = () => {
    if (onPress) {
      scale.value = withTiming(0.98, { duration: 100 });
    }
  };
  
  const handlePressOut = () => {
    if (onPress) {
      scale.value = withSpring(1, { damping: 15, stiffness: 200 });
    }
  };
  
  const Container = onPress ? AnimatedPressable : Animated.View;
  
  return (
    <Container
      style={[
        styles.container,
        styles[variant],
        styles[`padding_${padding}`],
        onPress && animatedStyle,
        style,
      ]}
      onPress={onPress}
      onPressIn={handlePressIn}
      onPressOut={handlePressOut}
      accessibilityRole={onPress ? 'button' : undefined}
    >
      {children}
    </Container>
  );
};

const styles = StyleSheet.create({
  container: {
    borderRadius: borderRadius.lg,
    overflow: 'hidden',
  },
  
  // Variants
  elevated: {
    backgroundColor: colors.surfaceElevated,
    ...shadows.sm,
  },
  filled: {
    backgroundColor: colors.neutral100,
  },
  outlined: {
    backgroundColor: colors.surfaceElevated,
    borderWidth: 1,
    borderColor: colors.border,
  },
  
  // Padding
  padding_none: {
    padding: 0,
  },
  padding_small: {
    padding: ms(12),
  },
  padding_medium: {
    padding: ms(16),
  },
  padding_large: {
    padding: ms(20),
  },
});
```
