# 인증 화면 (Auth Screens) - Production Ready v2.0

## 📁 파일 구조

```
01_auth/
├── README.md              # 개요 (이 파일)
├── login.md               # 로그인 화면
├── register.md            # 회원가입 화면
└── forgot-password.md     # 비밀번호 재설정 화면
```

---

## 🎨 디자인 원칙

### 공통 UX 목표

- **중앙 집중 레이아웃**: 모든 인증 화면은 콘텐츠를 중앙에 배치하여 집중도 향상
- **명확한 피드백**: 입력, 성공, 에러 상태를 즉시 시각적으로 표시
- **부드러운 전환**: 화면 간 자연스러운 애니메이션
- **접근성 우선**: 스크린 리더, 대형 터치 타겟 지원
- **키보드 친화적**: 키보드 내비게이션 최적화

### 레이아웃 원칙

모든 인증 화면은 다음 구조를 따릅니다:

```
┌───────────────────────────────────────────────────────────┐
│ StatusBar                                                 │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  Header (투명, 백 버튼만 또는 없음)                        │
│                                                           │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  KeyboardAvoidingView                                     │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │  ScrollView                                         │  │
│  │  contentContainerStyle:                             │  │
│  │    flexGrow: 1                                      │  │
│  │    justifyContent: center  ← 중앙 정렬              │  │
│  │    paddingHorizontal: screenPadding                 │  │
│  │                                                     │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │                                             │    │  │
│  │  │  아이콘 (80x80, brand100 bg)                │    │  │
│  │  │                                             │    │  │
│  │  │  제목 (displaySmall, 중앙)                  │    │  │
│  │  │                                             │    │  │
│  │  │  부제목 (bodyMedium, 중앙, textSecondary)   │    │  │
│  │  │                                             │    │  │
│  │  │  [폼 영역]                                  │    │  │
│  │  │                                             │    │  │
│  │  │  [버튼 영역]                                │    │  │
│  │  │                                             │    │  │
│  │  │  [하단 링크]                                │    │  │
│  │  │                                             │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## 화면 목록

| 화면 | 파일 | 설명 | 레이아웃 |
|------|------|------|---------|
| 로그인 | [login.md](./login.md) | 이메일/비밀번호 로그인 | 중앙 정렬, 단일 화면 |
| 회원가입 | [register.md](./register.md) | 신규 계정 생성, 역할 선택 | 중앙 정렬, 스텝 진행 |
| 비밀번호 재설정 | [forgot-password.md](./forgot-password.md) | 이메일로 인증번호 발송 | 중앙 정렬, 스텝 진행 |

---

## 네비게이션 흐름

```
┌─────────────────┐
│     로그인      │ ◄──────────────────┐
│     화면        │                    │
│                 │                    │
│  [중앙 정렬]    │                    │
└────────┬────────┘                    │
         │                             │
    ┌────┴────┐                        │
    ▼         ▼                        │
┌────────────────┐  ┌────────────────┐ │
│    회원가입    │  │  비밀번호 찾기 │ │
│                │  │                │ │
│  Step 1: 역할  │  │  Step 1: 이메일│ │
│  Step 2: 정보  │  │  Step 2: 인증  │ │
│  Step 3: 계정  │  │  Step 3: 재설정│ │
│  Step 4: 사업자│  │  완료 → 로그인 │─┘
│  Step 5: 프로필│  └────────────────┘
│  Step 6: 약관  │
│  완료 → 로그인 │─────────────────────┘
└────────────────┘
```

---

## 공통 컴포넌트

### 1. 아이콘 컨테이너

```typescript
const iconContainerStyles = {
  container: {
    width: ms(80),
    height: ms(80),
    borderRadius: ms(24),
    backgroundColor: colors.brand100,
    justifyContent: 'center',
    alignItems: 'center',
    alignSelf: 'center',
  },
  icon: {
    size: ms(40),
    color: colors.brand500,
  },
};
```

### 2. 입력 필드

```typescript
const inputStyles = {
  container: {
    height: ms(56),
    backgroundColor: colors.neutral100,
    borderRadius: borderRadius.md,
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: space4,
  },
  focused: {
    backgroundColor: colors.white,
    borderWidth: 2,
    borderColor: colors.brand500,
  },
  error: {
    borderWidth: 2,
    borderColor: colors.error,
  },
};
```

### 3. OTP 입력

```typescript
const otpStyles = {
  cell: {
    width: ms(48),
    height: ms(56),
    borderRadius: borderRadius.md,
    backgroundColor: colors.neutral100,
    justifyContent: 'center',
    alignItems: 'center',
  },
  gap: ms(8),
};
```

### 4. 스테퍼 / 프로그레스 바

```typescript
const stepperStyles = {
  bar: {
    height: ms(4),
    borderRadius: ms(2),
    backgroundColor: colors.neutral200,
  },
  fill: {
    backgroundColor: colors.brand500,
  },
};
```

---

## 공통 애니메이션

### 화면 진입

```typescript
const screenEnter = {
  icon: {
    from: { scale: 0.8, opacity: 0 },
    to: { scale: 1, opacity: 1 },
    duration: 400,
    delay: 100,
  },
  title: {
    from: { translateY: 20, opacity: 0 },
    to: { translateY: 0, opacity: 1 },
    duration: 400,
    delay: 200,
  },
  form: {
    from: { translateY: 20, opacity: 0 },
    to: { translateY: 0, opacity: 1 },
    duration: 300,
    delay: 300,
    stagger: 50,
  },
};
```

### 에러 피드백

```typescript
const errorFeedback = {
  shake: {
    translateX: [0, -8, 8, -8, 8, -4, 4, 0],
    duration: 400,
  },
  haptic: 'error',
};
```

### 성공 피드백

```typescript
const successFeedback = {
  scale: {
    from: 1,
    to: 1.05,
    return: 1,
    duration: 200,
  },
  haptic: 'success',
};
```

---

## 공통 유효성 검사

### 이메일

```typescript
const emailValidation = {
  pattern: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
  message: '올바른 이메일 형식을 입력해주세요',
};
```

### 비밀번호

```typescript
const passwordValidation = {
  minLength: 8,
  requirements: {
    length: { pattern: /.{8,}/, message: '8자 이상' },
    letter: { pattern: /[a-zA-Z]/, message: '영문 포함' },
    number: { pattern: /[0-9]/, message: '숫자 포함' },
    special: { pattern: /[!@#$%^&*(),.?":{}|<>]/, message: '특수문자 포함' },
  },
};
```

### 인증번호

```typescript
const codeValidation = {
  length: 6,
  expiryTime: 180, // 3분
  resendCooldown: 60, // 60초
  maxAttempts: 5,
};
```

---

## 공통 에러 처리

| 에러 타입 | 표시 방식 | 애니메이션 |
|---------|---------|----------|
| 필드 에러 | 인라인 | 페이드인 |
| 인증 실패 | 토스트 | 슬라이드 다운 |
| 네트워크 오류 | 토스트 + 재시도 버튼 | 슬라이드 다운 |
| 서버 오류 | 토스트 | 슬라이드 다운 |

---

## 공통 접근성

```typescript
const accessibility = {
  // 입력 필드
  input: {
    accessibilityLabel: '필드명',
    accessibilityHint: '입력 안내',
  },
  
  // 버튼
  button: {
    accessibilityRole: 'button',
    accessibilityState: { disabled, busy },
  },
  
  // 에러 메시지
  error: {
    accessibilityRole: 'alert',
    accessibilityLiveRegion: 'polite',
  },
  
  // 스테퍼
  stepper: {
    accessibilityRole: 'progressbar',
    accessibilityLabel: '진행 상태',
    accessibilityValue: { now, min, max },
  },
};
```

---

## UX 특징

### 20~30대 근무자 배려
- 빠른 로그인 (자동완성 지원)
- 직관적인 UI (아이콘, 시각적 피드백)
- 스마트폰 최적화 (큰 터치 타겟)

### 50~60대 관리자 배려
- 큰 텍스트 (최소 15px)
- 충분한 여백
- 명확한 상태 표시
- 단계별 안내
