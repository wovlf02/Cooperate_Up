# 📢 공지사항 도메인

## 개요

스터디 내 공지사항 관리 기능입니다. 공지 작성, 조회, 수정, 삭제, 고정 기능을 제공합니다.

---

## 주요 기능

| 기능 | 설명 |
|------|------|
| 공지 조회 | 페이지네이션, 필터링 |
| 공지 작성 | ADMIN 이상 권한 필요 |
| 공지 수정 | 작성자 또는 ADMIN 이상 |
| 공지 삭제 | 작성자 또는 ADMIN 이상 |
| 공지 고정 | 최대 3개, ADMIN 이상 |
| 조회수 | 상세 조회 시 자동 증가 |

---

## 아키텍처

```
┌────────────────────────────────────────────────────┐
│                    Client                          │
│  ┌──────────────────────────────────────────────┐  │
│  │         NoticesPage (page.jsx)               │  │
│  │  - 공지사항 목록                              │  │
│  │  - 필터 탭 (전체/고정/중요/일반)              │  │
│  │  - 작성/수정 모달                             │  │
│  │  - 상세 모달                                  │  │
│  └──────────────────────────────────────────────┘  │
│                        │                           │
│                        ▼                           │
│  ┌──────────────────────────────────────────────┐  │
│  │          useApi Hooks                        │  │
│  │  - useNotices()                              │  │
│  │  - useCreateNotice()                         │  │
│  │  - useUpdateNotice()                         │  │
│  │  - useDeleteNotice()                         │  │
│  │  - useTogglePinNotice()                      │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────┬──────────────────────────┘
                          │
                          ▼
┌────────────────────────────────────────────────────┐
│                     Server                         │
│  ┌──────────────────────────────────────────────┐  │
│  │       /api/studies/[id]/notices              │  │
│  │  - GET:  공지 목록 조회                       │  │
│  │  - POST: 공지 생성                            │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │   /api/studies/[id]/notices/[noticeId]       │  │
│  │  - GET:    공지 상세 (조회수 증가)            │  │
│  │  - PATCH:  공지 수정                          │  │
│  │  - DELETE: 공지 삭제                          │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │   /api/studies/[id]/notices/[noticeId]/pin   │  │
│  │  - POST: 고정/해제 토글                       │  │
│  └──────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────┘
```

---

## 파일 구조

```
coup/src/
├── app/
│   ├── api/studies/[id]/notices/
│   │   ├── route.js              # GET(목록), POST(생성)
│   │   └── [noticeId]/
│   │       ├── route.js          # GET(상세), PATCH(수정), DELETE(삭제)
│   │       └── pin/
│   │           └── route.js      # POST(고정 토글)
│   └── my-studies/[studyId]/notices/
│       ├── page.jsx              # 공지사항 화면
│       └── page.module.css       # 스타일
├── components/studies/
│   └── NoticeCreateEditModal.jsx # 작성/수정 모달
└── lib/
    ├── hooks/useApi.js           # useNotices, useCreateNotice, ...
    └── exceptions/study/
        └── StudyException.js     # StudyNoticeException
```

---

## 데이터베이스 모델

### Notice 모델

```prisma
model Notice {
  id          String   @id @default(cuid())
  studyId     String
  authorId    String
  title       String
  content     String   @db.Text
  isPinned    Boolean  @default(false)
  isImportant Boolean  @default(false)
  views       Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  study       Study    @relation(fields: [studyId], references: [id], onDelete: Cascade)
  author      User     @relation(fields: [authorId], references: [id])

  @@index([studyId, isPinned])
}
```

---

## 공지 유형

| 유형 | 설명 | 표시 위치 |
|------|------|-----------|
| 고정 (isPinned) | 상단 고정 | 목록 최상단 |
| 중요 (isImportant) | 중요 표시 | 하이라이트 |
| 일반 | 기본 공지 | 일반 목록 |

---

## 권한

| 역할 | 조회 | 작성 | 수정 | 삭제 | 고정 |
|------|------|------|------|------|------|
| OWNER | ✅ | ✅ | ✅ 모두 | ✅ 모두 | ✅ |
| ADMIN | ✅ | ✅ | ✅ 모두 | ✅ 모두 | ✅ |
| MEMBER | ✅ | ❌ | ❌ | ❌ | ❌ |

---

## 관련 문서

- [API](./api.md)
- [화면](./screens.md)
- [예외](./exceptions.md)

