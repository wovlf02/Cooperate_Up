# 📱 스터디 상세 화면

## 개요

스터디의 상세 정보를 확인하고 가입을 신청하는 화면입니다.

---

## 접근 경로

| 항목 | 값 |
|------|-----|
| **URL** | `/studies/[studyId]` |
| **라우트 파일** | `src/app/studies/[studyId]/page.jsx` |

---

## 레이아웃 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                         Header                                  │
├──────────┬──────────────────────────────────────────────────────┤
│          │                                                      │
│ Sidebar  │  ┌────────────────────────────────────────────────┐  │
│          │  │  💻 JavaScript 스터디                          │  │
│          │  │  프로그래밍 | 모집중                           │  │
│          │  │  5/10명                                        │  │
│          │  │  [가입 신청]                                   │  │
│          │  └────────────────────────────────────────────────┘  │
│          │                                                      │
│          │  ┌─────────────────────────────────────┐             │
│          │  │  📖 소개                            │             │
│          │  │  JS 심화 학습을 위한 스터디입니다.  │             │
│          │  │  ...                                │             │
│          │  └─────────────────────────────────────┘             │
│          │                                                      │
│          │  ┌─────────────────────────────────────┐             │
│          │  │  👥 멤버 (5명)                      │             │
│          │  │  👑 홍길동 | 김철수 | 이영희 | ... │             │
│          │  └─────────────────────────────────────┘             │
│          │                                                      │
│          │  ┌─────────────────────────────────────┐             │
│          │  │  🏷️ 태그                            │             │
│          │  │  #javascript #frontend #react       │             │
│          │  └─────────────────────────────────────┘             │
│          │                                                      │
└──────────┴──────────────────────────────────────────────────────┘
```

---

## 영역별 상세

### 헤더 영역

| 요소 | 설명 |
|------|------|
| 이모지 + 이름 | 스터디 이모지와 이름 |
| 카테고리 | 분류 뱃지 |
| 모집 상태 | 모집중/마감 뱃지 |
| 멤버 수 | 현재/최대 인원 |
| 액션 버튼 | 멤버 여부에 따라 다름 |

### 액션 버튼 상태

| 상태 | 버튼 |
|------|------|
| 비멤버 + 모집중 | [가입 신청] |
| 비멤버 + 마감 | [모집 마감] (비활성) |
| 승인 대기 | [승인 대기 중] (비활성) |
| 멤버 | [스터디 바로가기] |

### 소개 섹션

- 스터디 설명 (마크다운 지원)
- 스터디 규칙 (선택적)

### 멤버 섹션

- 전체 멤버 수
- 멤버 아바타 목록 (최대 표시 후 +N)
- OWNER는 👑 표시

### 태그 섹션

- 관련 태그 목록
- 클릭 시 해당 태그로 검색

---

## 상태 관리

```javascript
const { studyId } = useParams()
const { data, isLoading } = useStudy(studyId)
const { data: memberStatus } = useCheckMember(studyId)

const study = data?.data
const isMember = memberStatus?.isMember
const isPending = memberStatus?.status === 'PENDING'
```

---

## API 호출

| API | 시점 | 용도 |
|-----|------|------|
| GET `/api/studies/[id]` | 마운트 | 스터디 정보 |
| GET `/api/studies/[id]/check-member` | 마운트 | 멤버 여부 |
| POST `/api/studies/[id]/join` | 버튼 클릭 | 가입 신청 |

---

## 가입 신청 처리

```javascript
const handleJoin = async () => {
  try {
    const response = await fetch(`/api/studies/${studyId}/join`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ introduction, motivation, level })
    })
    
    if (response.ok) {
      const result = await response.json()
      if (result.data.status === 'ACTIVE') {
        // 바로 스터디 대시보드로
        router.push(`/my-studies/${studyId}`)
      } else {
        // 승인 대기 안내
        toast.info('가입 신청이 완료되었습니다.')
      }
    }
  } catch (error) {
    toast.error(error.message)
  }
}
```

---

## 관련 문서

- [목록 화면](./screens-list.md) - 스터디 목록
- [가입 화면](./screens-join.md) - 가입 폼
- [상세 컴포넌트](./components-detail.md) - 컴포넌트 상세

