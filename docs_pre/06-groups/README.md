# 👥 그룹 도메인 개요

## 개요

그룹 생성, 탐색, 가입, 관리 등 그룹의 핵심 기능을 다루는 도메인입니다.  
스터디와 유사하지만 더 범용적인 커뮤니티 그룹을 위한 기능을 제공합니다.

---

## 주요 기능

| 기능 | 설명 |
|------|------|
| 그룹 생성 | 새 그룹 생성 및 초기 설정 |
| 그룹 탐색 | 카테고리, 검색을 통한 그룹 찾기 |
| 그룹 상세 | 그룹 정보 조회 |
| 가입/탈퇴 | 그룹 가입 신청 및 탈퇴 |
| 멤버 관리 | 멤버 추가, 역할 변경, 강퇴 |
| 그룹 설정 | 공개 여부, 모집 상태 등 설정 |
| 초대 관리 | 초대 코드 생성 및 공유 |

---

## 관련 파일

### API

| 경로 | 설명 |
|------|------|
| `src/app/api/groups/route.js` | 그룹 목록 조회, 생성 |
| `src/app/api/groups/[id]/route.js` | 그룹 상세, 수정, 삭제 |
| `src/app/api/groups/[id]/join/route.js` | 가입 처리 |
| `src/app/api/groups/[id]/leave/route.js` | 탈퇴 처리 |
| `src/app/api/groups/[id]/members/route.js` | 멤버 관리 |
| `src/app/api/groups/[id]/invites/route.js` | 초대 관리 |
| `src/app/api/groups/search/route.js` | 고급 검색 |

### 헬퍼/예외

| 경로 | 설명 |
|------|------|
| `src/lib/helpers/group-helpers.js` | 그룹 헬퍼 함수 |
| `src/lib/validators/group-validators.js` | 입력 검증 |
| `src/lib/exceptions/group/` | 예외 클래스 |
| `src/lib/logging/groupLogger.js` | 로깅 시스템 |

---

## 그룹 모델 요약

```prisma
model Group {
  id           String   @id @default(uuid())
  name         String
  description  String?
  category     String   @default("etc")
  isPublic     Boolean  @default(true)
  isRecruiting Boolean  @default(true)
  maxMembers   Int      @default(50)
  imageUrl     String?
  createdBy    String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  deletedAt    DateTime?
  
  members      GroupMember[]
  invites      GroupInvite[]
}
```

---

## 멤버 역할 및 권한

| 역할 | 설정 변경 | 멤버 관리 | 멤버 추가 | 그룹 삭제 |
|------|----------|----------|----------|----------|
| `OWNER` | ✓ | ✓ | ✓ | ✓ |
| `ADMIN` | ✓ | ✓ | ✓ | ✗ |
| `MEMBER` | ✗ | ✗ | ✗ | ✗ |

---

## 가입 처리 흐름

### 공개 그룹 (isPublic: true)

```
사용자 → [가입 신청] → 즉시 ACTIVE → 완료
```

### 비공개 그룹 (isPublic: false)

```
사용자 → [가입 신청] → PENDING → 관리자 승인 → ACTIVE
```

### 초대 코드 사용

```
사용자 → [초대 코드 입력] → 즉시 ACTIVE → 완료
```

---

## 상태 정의

### 그룹 상태

| 필드 | 값 | 설명 |
|------|-----|------|
| `isPublic` | true/false | 검색 노출 및 자유 가입 여부 |
| `isRecruiting` | true/false | 신규 멤버 모집 여부 |
| `deletedAt` | null/Date | 소프트 삭제 |

### 멤버 상태

| 상태 | 설명 |
|------|------|
| `PENDING` | 가입 승인 대기 (비공개 그룹) |
| `ACTIVE` | 활성 멤버 |
| `KICKED` | 강퇴됨 (재가입 불가) |
| `LEFT` | 자발적 탈퇴 (재가입 가능) |

---

## 카테고리

| 값 | 설명 |
|-----|------|
| `study` | 스터디 |
| `project` | 프로젝트 |
| `hobby` | 취미 |
| `network` | 네트워킹 |
| `social` | 소셜 |
| `etc` | 기타 (기본값) |

---

## 에러 코드 범위

| 범위 | 분류 |
|------|------|
| GROUP-001 ~ GROUP-020 | 검증 예외 |
| GROUP-021 ~ GROUP-040 | 권한 예외 |
| GROUP-041 ~ GROUP-060 | 멤버 예외 |
| GROUP-061 ~ GROUP-076 | 비즈니스 예외 |

---

## 관련 문서

- [API](./api.md) - 그룹 API 문서
- [멤버 API](./api-members.md) - 멤버 관리 API
- [헬퍼](./helpers.md) - 헬퍼 함수
- [예외](./exceptions.md) - 예외 클래스
- [검증](./validators.md) - 입력 검증

